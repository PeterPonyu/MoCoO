%% MoCoO: Momentum Contrast ODE-Regularized VAE for Single-Cell
%% Trajectory Inference and Representation Learning
%%
%% Prepared for IEEE Journal of Biomedical and Health Informatics (J-BHI)
%% Using IEEEtran document class
%%
\documentclass[journal]{IEEEtran}

%% Packages
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{makecell}
\usepackage{xspace}
\usepackage{bm}
\usepackage{balance}
\usepackage{tikz}

%% Graphics search path (generated figures live in benchmarks/figures/)
\graphicspath{{../benchmarks/figures/}{./}}

%% hyperref should be loaded last
\usepackage{hyperref}

%% Custom commands
\newcommand{\MoCoO}{\textsc{MoCoO}\xspace}
\newcommand{\ie}{\textit{i.e.}\xspace}
\newcommand{\eg}{\textit{e.g.}\xspace}

%% Moderate white-space reduction
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{2pt}
\setlength{\abovedisplayskip}{6pt plus 2pt minus 2pt}
\setlength{\belowdisplayskip}{6pt plus 2pt minus 2pt}
\setlength{\abovedisplayshortskip}{3pt plus 1pt minus 1pt}
\setlength{\belowdisplayshortskip}{3pt plus 1pt minus 1pt}

\begin{document}

\title{MoCoO: Momentum Contrast ODE-Regularized VAE for Single-Cell Trajectory Inference and Representation Learning}

\author{Zeyu~Fu%
\thanks{Manuscript submitted to IEEE Journal of Biomedical and Health Informatics.}%
\thanks{Z. Fu is with the School of Computer Science, University of Birmingham, Birmingham, B15 2TT, UK. (email: zxf123@student.bham.ac.uk)}
}

\markboth{IEEE Journal of Biomedical and Health Informatics}%
{Fu: MoCoO --- Momentum Contrast ODE-Regularized VAE for Single-Cell Trajectory Inference}

\maketitle

\begin{abstract}
Characterising cellular differentiation from single-cell RNA sequencing (scRNA-seq) requires representations that capture both discrete cell-type identity and continuous developmental trajectories. We present \MoCoO, a modular framework integrating a Variational Autoencoder (VAE), Neural Ordinary Differential Equations (Neural ODE), and Momentum Contrast (MoCo), complemented by a systematic Phase-2 Flow Matching (FM) refinement step applicable to all model variants. Through a systematic six-configuration ablation across 20 scRNA-seq datasets evaluated with a proposed five-metric suite covering clustering geometry (ASW, DAV, CAL) and embedding quality (DRE, DREX), we demonstrate two central findings. First, the ODE+MoCo combination is the core architectural synergy: VAE+ODE+MoCo achieves four of five top-two finishes among base configurations, including the best ASW ($0.225$) and DAV ($1.478$), plus second-best DRE ($0.640$) and CAL. Second, FM refinement systematically improves both embedding quality and clustering geometry across all six base configurations---DREX in 92\% and DRE in 88\% of 120 dataset--configuration pairs ($\Delta$DREX$=+0.030$, $\Delta$DRE$=+0.023$), CAL in 88\%, ASW in 86\% ($\Delta$ASW$=+0.018$), and DAV in 80\% ($\Delta$DAV$=-0.072$; Fig.~\ref{fig:ablation_boxplots}). Combined, the full MoCoO pipeline (VAE+ODE+MoCo+Proto+FM) achieves the best DRE ($0.678$), DREX ($0.660$), and CAL, while VAE+ODE+MoCo+FM achieves the best ASW ($0.257$) and DAV ($1.359$). ODE smooths the latent manifold along developmental trajectories; MoCo sharpens cluster geometry; FM recovers and amplifies both embedding quality and cluster separation post-hoc. Downstream validation confirms that MoCoO latent spaces support annotation transfer, uncertainty quantification, differential expression, and branching detection. Pseudotime predictions correlate significantly with canonical marker genes across all five core developmental systems. We publicly release the MoCoO Python package (\texttt{pip install mocoo}) and full benchmark suite.
\end{abstract}

\begin{IEEEkeywords}
Single-cell RNA sequencing, variational autoencoder, neural ordinary differential equations, momentum contrast, contrastive learning, trajectory inference, pseudotime, representation learning, computational biology.
\end{IEEEkeywords}

%% ============================================================
\section{Introduction}
\label{sec:introduction}
%% ============================================================

Single-cell RNA sequencing (scRNA-seq) has transformed our understanding of cellular heterogeneity by enabling transcriptome-wide profiling at single-cell resolution~\cite{tang2009mrna}. A fundamental analysis task is to learn low-dimensional representations that faithfully capture both \textit{discrete} cell-type identity and \textit{continuous} developmental dynamics such as differentiation trajectories~\cite{kiselev2019challenges,trapnell2014dynamics}. These representations underpin downstream tasks including clustering, pseudotime inference, RNA velocity estimation, and differential expression analysis~\cite{wolf2018scanpy}.

Variational Autoencoders (VAEs) have become the de facto standard for scRNA-seq dimensionality reduction, with methods such as scVI~\cite{lopez2018deep} and scVAE~\cite{gronbech2020scvae} demonstrating strong performance through count-based likelihood models (negative binomial, ZINB). However, standard VAE latent spaces tend to be over-smoothed, conflating nearby cell states. Two complementary strategies address this limitation.

First, Neural Ordinary Differential Equations (Neural ODEs)~\cite{chen2018neural} provide a principled framework for modelling continuous-time dynamics in latent space. By parameterising the latent derivative with a neural network and integrating via an ODE solver, these models learn smooth trajectories without discrete time-step assumptions. Latent ODE models~\cite{rubanova2019latent} have shown promise for time-series modelling, and recent work has adapted them to single-cell contexts for pseudotime inference and RNA velocity estimation~\cite{fu2025panode}.

Second, contrastive learning---particularly Momentum Contrast (MoCo)~\cite{he2020momentum}---learns representations that are locally smooth yet globally discriminative. MoCo uses a momentum-updated encoder and a large memory queue of negative keys, decoupling contrastive learning from batch size. Prototype contrastive learning~\cite{li2021prototypical} further imposes cluster structure by aligning representations with learnable prototype vectors.

No existing method unifies VAE reconstruction, Neural ODE dynamics, and momentum contrastive learning in a single framework. We propose \MoCoO (\textbf{Mo}mentum \textbf{Co}ntrast \textbf{O}DE-regularized VAE), a modular architecture that combines all three paradigms:

\begin{enumerate}
    \item \textbf{VAE with flexible count-based likelihoods} (MSE, NB, ZINB, Poisson, ZIP) provides a probabilistic latent space that handles zero-inflation and overdispersion in scRNA-seq counts.
    \item \textbf{Neural ODE regularisation} models continuous developmental dynamics, derives unsupervised pseudotime ordering, and produces gradient-based RNA velocity estimates.
    \item \textbf{Momentum Contrast with prototype heads} enforces instance-level discrimination via a momentum encoder and memory queue, with prototype learning that aligns representations to learnable cluster centres.
    \item \textbf{Information bottleneck} applies a secondary low-dimensional projection for hierarchical feature extraction.
\end{enumerate}
The framework additionally supports DIP-VAE, $\beta$-TC-VAE, and InfoVAE/MMD disentanglement regularisers, but these are not active in the experiments reported here.

We conduct a systematic ablation across six configurations (VAE, VAE+ODE, VAE+MoCo, VAE+MoCo+Proto, VAE+ODE+MoCo, and VAE+ODE+MoCo+Proto) on 20 scRNA-seq datasets spanning diverse developmental systems, with a Phase-2 Flow Matching (FM) refinement step that extends this to 12 configurations. Deep-learning external baselines with comparable latent representations---scVI~\cite{lopez2018deep}, Harmony~\cite{korsunsky2019harmony}, and scANVI~\cite{xu2021probabilistic}---contextualise MoCoO's absolute performance. A key finding is that combining ODE and MoCo consistently produces the best cluster geometry (cross-dataset mean ASW $0.225$, DAV $1.478$ for VAE+ODE+MoCo), while VAE+ODE achieves the best embedding quality (DRE $0.647$, DREX $0.613$). ODE-containing configurations consistently outperform non-ODE variants on cluster geometry across all 20 datasets ($\Delta$ASW ${\sim}0.04$, $\Delta$DAV ${\sim}0.23$). FM refinement improves distance-preservation metrics in over 85\% of dataset--configuration pairs (DREX $\Delta{=}+0.030$, DRE $\Delta{=}+0.023$). Component analysis reveals that each module contributes complementary geometric structure: the Neural ODE smooths the latent manifold along trajectories and MoCo sharpens pairwise distance relationships, with the ODE+MoCo combination producing the strongest overall synergy across all 20 datasets. Evaluation is centred on a proposed five-metric suite covering clustering geometry (ASW, Davies--Bouldin, Calinski--Harabasz) and embedding quality (DRE, DREX), complemented by biological validation (pseudotime--marker correlations), downstream analysis (annotation transfer, uncertainty quantification, generation quality, gene importance, branching detection, differential expression), batch integration (iLISI, bASW, cLISI, graph connectivity, isolated label ASW via scIB~\cite{luecken2022scib}), and training dynamics.

%% ============================================================
\section{Related Work}
\label{sec:related}
%% ============================================================

\subsection{Variational Autoencoders for scRNA-seq}
\label{sec:related:vae}

scVI~\cite{lopez2018deep} introduced the negative binomial VAE for scRNA-seq, jointly modelling library size and batch effects. Extensions include scVAE~\cite{gronbech2020scvae} (multiple count distributions), scANVI~\cite{xu2021probabilistic} (semi-supervised cell-type annotation), and totalVI~\cite{gayoso2021joint} (multi-omics). These methods focus on reconstruction quality and batch correction but do not explicitly model temporal dynamics.

\subsection{Neural ODEs and Latent ODE Models}
\label{sec:related:ode}

Neural ODEs~\cite{chen2018neural} parameterise continuous dynamical systems $\frac{dz}{dt} = f_\theta(z, t)$ and integrate using adaptive ODE solvers. The Latent ODE framework~\cite{rubanova2019latent} combines a VAE encoder with an ODE-governed latent space for irregularly sampled time series. Applications to single-cell biology include trajectory inference~\cite{fu2025panode}, while related dynamical modelling approaches have advanced RNA velocity estimation~\cite{bergen2020generalizing}. Optimal-transport-based methods such as TrajectoryNet~\cite{tong2020trajectorynet} and CellOT~\cite{bunne2023cellot} model population-level dynamics rather than single-cell ODE trajectories. MoCoO extends the PanODE framework~\cite{fu2025panode} (by the first author). Specifically, MoCoO inherits the VAE encoder--decoder, Neural ODE integration, and pseudotime-prediction head from PanODE. The novel contributions of the current work are: (i)~the MoCo module with momentum encoder and memory queue, (ii)~learnable prototype heads for cluster anchoring, (iii)~the information bottleneck, (iv)~the systematic six-configuration ablation design, and (v)~the DRE/DREX evaluation suites. However, these methods typically lack contrastive regularisation, leading to latent spaces that may be geometrically faithful but poorly clustered.

\subsection{Contrastive Learning for Single-Cell Data}
\label{sec:related:contrastive}

MoCo~\cite{he2020momentum} and SimCLR~\cite{chen2020simple} have been adapted to biological contexts. scAGCL~\cite{cai2024scagcl} applies symmetric augmentation-guided contrastive learning to scRNA-seq data with cell-type-aware positive pair construction. scGPCL~\cite{cai2024scgpcl} introduces prototype contrastive learning that aligns cell embeddings with learnable prototype vectors representing cell types. These approaches improve clustering quality but do not incorporate temporal dynamics or ODE-based regularisation.

\subsection{Integrated Approaches}
\label{sec:related:integrated}

Several methods combine two of the three paradigms. scDiff~\cite{hao2024scdiff} uses diffusion models (related to score-based SDEs) for single-cell generation. VeloVAE~\cite{li2024velovae} integrates VAE with RNA velocity. Monocle~3~\cite{cao2019monocle3} and PAGA~\cite{wolf2019paga} use graph abstractions for trajectory inference. For batch integration, Harmony~\cite{korsunsky2019harmony} applies iterative soft clustering in PCA space to remove batch effects. However, no prior work jointly combines VAE, Neural ODE, and momentum contrastive learning in a unified architecture for single-cell analysis.

%% ============================================================
\section{Method}
\label{sec:method}
%% ============================================================

\subsection{Overview}
\label{sec:method:overview}

MoCoO (Fig.~\ref{fig:architecture}) processes scRNA-seq count data $X \in \mathbb{R}^{N \times G}$ ($N$ cells, $G$ genes) through five components: (1)~a VAE with count-based decoder, (2)~a Neural ODE for continuous dynamics, (3)~a Momentum Contrast module with memory queue, (4)~an information bottleneck, and (5)~optional disentanglement regularisers.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{fig_architecture}
    \caption{\MoCoO architecture overview (\textbf{Fig.~1}). Count data $X \in \mathbb{R}^{N \times G}$ passes through a VAE encoder to produce latent representations~$z$, refined by a Neural ODE solver (orange) for continuous trajectory modelling. Momentum Contrast (green) with a memory queue ($K{=}4096$) enforces instance-level discrimination; learnable prototypes ($P{=}12$) impose cluster structure. An information bottleneck (purple) provides hierarchical compression to $d_{\mathrm{ib}}{=}4$. Dashed regions denote modular components. Phase-2 FM refinement (vermilion, right) operates post-training on the frozen latent space of any configuration—the central contribution of this work. Colour coding is consistent throughout all figures.}
    \label{fig:architecture}
\end{figure*}

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{fig2_ablation_hero}
    \caption{FM systematically improves all model variants across all metrics (\textbf{Fig.~2}). Each subplot shows absolute performance for all 12 configurations (6 base + 6~FM variants) across 20 datasets. Coloured triangle indicators above each pair show FM effect: \textcolor[RGB]{44,160,44}{$\blacktriangle$} = FM improved, \textcolor[RGB]{214,39,40}{$\blacktriangledown$} = FM degraded. Subplot titles include FM win-rate (\% of 120 config$\times$dataset pairs improved). ODE-containing configurations (V+OM, Full) dominate all metrics, and FM universally raises the median across all 6 base variants (CAL 88\%, DREX 92\%, DRE 88\%, ASW 86\%, DAV 80\%).}
    \label{fig:ablation_boxplots}
\end{figure*}

\subsection{Encoder}
\label{sec:method:encoder}

The encoder $q_\phi(z|x)$ maps log-normalised expression $\tilde{x} = \log(1 + x)$ through a two-layer ReLU MLP to produce mean $\mu$ and pre-softplus scale $\sigma'$ of a diagonal Gaussian posterior~\eqref{eq:encoder}; latent samples are drawn via reparameterisation~\eqref{eq:reparam}:
\begin{equation}
    q_\phi(z|x) = \mathcal{N}\!\left(z;\, \mu_\phi(\tilde{x}),\, \mathrm{softplus}(\sigma'_\phi(\tilde{x}))^2 I\right),
    \label{eq:encoder}
\end{equation}
\begin{equation}
    z = \mu + \mathrm{softplus}(\sigma') \cdot \epsilon, \quad \epsilon \sim \mathcal{N}(0, I).
    \label{eq:reparam}
\end{equation}

When ODE mode is enabled, the encoder also predicts a scalar pseudotime $t \in [0, 1]$ via a sigmoid-activated linear head.

\subsection{Neural ODE Component}
\label{sec:method:ode}

Given latent samples $\{z_i\}$ and predicted pseudotimes $\{t_i\}$, the ODE module orders cells by pseudotime and solves~\eqref{eq:ode}:
\begin{equation}
    \frac{dz}{dt} = f_\psi(z, t), \quad z(t_0) = z_{\mathrm{init}},
    \label{eq:ode}
\end{equation}
where $f_\psi$ is a two-layer ELU MLP. Integration uses \texttt{torchdiffeq}'s fixed-step RK4 solver, chosen over adaptive methods (e.g., Dormand--Prince~\cite{dormand1980rk}) because the encoder-predicted pseudotime grid is relatively smooth and fixed-step integration provides deterministic gradients without adaptive step-size overhead---important for stable joint optimisation of the encoder and ODE function. Preliminary comparisons showed negligible clustering geometry differences ($\Delta$ASW$<0.005$, $\Delta$DAV$<0.02$) between RK4 and Dormand--Prince adaptive integration, with $\sim$30\% longer wall-clock time for the adaptive solver due to step-size adaptation overhead in the joint training loop. The ODE representations $z_{\mathrm{ode}}$ blend with VAE latent samples via configurable weights~\eqref{eq:blend}:
\begin{equation}
    z_{\mathrm{blend}} = \alpha_{\mathrm{vae}} \cdot z + \alpha_{\mathrm{ode}} \cdot z_{\mathrm{ode}}.
    \label{eq:blend}
\end{equation}

Two auxiliary losses regularise the ODE:
\begin{itemize}
    \item \textbf{ODE--VAE alignment:} $\mathcal{L}_{\mathrm{ode}} = \| z - z_{\mathrm{ode}} \|_2^2$
    \item \textbf{Velocity consistency:} $\mathcal{L}_{\mathrm{vel}} = 1 - \cos(z_{i+1} - z_i,\, f_\psi(z_i, t_i))$, aligning the learned velocity field with empirical displacements.
\end{itemize}

\subsection{Momentum Contrast Module}
\label{sec:method:moco}

The MoCo module follows MoCo~v1~\cite{he2020momentum} with enhancements from scAGCL~\cite{cai2024scagcl} and scGPCL~\cite{cai2024scgpcl}:

\begin{enumerate}
    \item \textbf{Query and Key Encoders:} The query encoder shares parameters with the VAE encoder; the key encoder is updated via EMA: $\theta_k \leftarrow m \cdot \theta_k + (1 - m) \cdot \theta_q$, $m = 0.999$.
    \item \textbf{Projection Heads:} Two-layer MLPs (BatchNorm, ReLU) map $d$-dimensional latent vectors to a $d_{\mathrm{proj}}$-dimensional contrastive space.
    \item \textbf{Memory Queue:} A FIFO buffer of size $K$ stores projected keys, decoupling the number of negatives from batch size.
    \item \textbf{InfoNCE Loss} (see~\eqref{eq:infonce}):
    \begin{equation}
        \mathcal{L}_{\mathrm{moco}} \!=\! -\log \frac{\exp(q \!\cdot\! k^+ / \tau)}{\exp(q \!\cdot\! k^+ / \tau) + \!\sum_{k^-} \exp(q \!\cdot\! k^- / \tau)},
        \label{eq:infonce}
    \end{equation}
    where the sum ranges over all $k^-$ in the memory queue and $\tau$ is the temperature.
    \item \textbf{Prototype Contrastive Loss} (optional; see~\eqref{eq:proto}):
    \begin{equation}
        \mathcal{L}_{\mathrm{proto}} = -\frac{1}{N} \sum_{i=1}^{N} \log \frac{\exp(z_i \cdot p_{c(i)} / \tau)}{\sum_{j=1}^{P} \exp(z_i \cdot p_j / \tau)},
        \label{eq:proto}
    \end{equation}
    where $\{p_j\}_{j=1}^{P}$ are learnable prototype vectors and $c(i) = \arg\max_j\, z_i \cdot p_j / \tau$ assigns cell $i$ to its nearest prototype.
\end{enumerate}

\textbf{Positive pair construction.} For each mini-batch, two stochastic augmented views of the same cell form positive pairs: the query view passes through the main encoder, while the key view passes through the momentum encoder. The augmentation pipeline applies three transformations, each with probability $p_{\mathrm{aug}}{=}0.5$: (1)~gene masking---randomly zeroing out genes with probability 0.1; (2)~Gaussian noise---adding $\mathcal{N}(0,\,0.04)$ to randomly selected genes (probability 0.1); (3)~feature swapping---exchanging gene values across cells in the batch (probability 0.05). Negatives come from all keys stored in the memory queue.

\textit{Augmentation rationale:} Gene masking simulates dropout, which is a dominant noise source in scRNA-seq protocols~\cite{hicks2018missing}. Gaussian noise ($\sigma{=}0.2$) models technical variability in library preparation. Feature swapping operates at very low probability ($p{=}0.05$, affecting ${\sim}5\%$ of genes per cell), which is equivalent to a regularised mixup---it encourages invariance to minor expression perturbations without creating biologically implausible hybrid profiles. All three augmentations are deliberately conservative: masking affects only 10\% of genes per view, noise is applied independently per gene, and swapping probability is below the natural inter-cell variance within a cell type. This design follows the principles validated in scAGCL~\cite{cai2024scagcl} and scGPCL~\cite{cai2024scgpcl}, which demonstrated that moderate stochastic augmentations improve contrastive learning for scRNA-seq without distorting cell-state semantics.

\subsection{Decoder}
\label{sec:method:decoder}

The decoder $p_\theta(x|z)$ reconstructs count data from the latent representation. It predicts normalised mean parameters $\rho = \mathrm{softmax}(\mathrm{MLP}(z))$ and uses gene-specific learnable dispersions $r$:
\begin{itemize}
    \item \textbf{Negative Binomial (NB):} $p(x_g | z) = \mathrm{NB}(x_g;\, \mu_g = l \cdot \rho_g,\, r_g)$
    \item \textbf{Zero-Inflated NB (ZINB):} $p(x_g | z) = \pi_g \cdot \delta_0(x_g) + (1 - \pi_g) \cdot \mathrm{NB}(x_g;\, \mu_g,\, r_g)$
\end{itemize}
where $l = \sum_g x_g$ is the library size and $\pi_g$ is a dropout probability predicted by a secondary decoder head. While MoCoO supports MSE, NB, ZINB, Poisson, and ZIP likelihoods, all experiments in this paper use the negative binomial (NB) likelihood unless otherwise stated.

\subsection{Information Bottleneck}
\label{sec:method:ib}

A secondary encoder-decoder pair maps $z \in \mathbb{R}^{d}$ to a bottleneck $b \in \mathbb{R}^{d_{\mathrm{ib}}}$ ($d_{\mathrm{ib}} = 4 < d$) and back to $\hat{z} \in \mathbb{R}^{d}$, imposing hierarchical compression that retains only the most salient features. The bottleneck reconstruction loss $\mathcal{L}_{\mathrm{irecon}} = \|\hat{z} - z\|_2^2$ is included in the total loss~\eqref{eq:total_loss} with weight $\lambda_{\mathrm{irecon}} = 1.0$. The IB serves as a fixed architectural component that forces the model to identify a low-dimensional summary of the latent representation; it is not ablated independently because preliminary experiments showed its effect to be minor compared to ODE and MoCo (clustering geometry changes $< 0.005$ ASW, $< 0.02$ DAV), and it adds negligible computational cost.

\subsection{Flow Matching Refinement}
\label{sec:method:fm}

As a systematic post-training enhancement, MoCoO includes a \emph{Phase-2 Flow Matching (FM)} refinement step that operates on the frozen latent space of any base configuration. Given the trained VAE encoder, FM learns a continuous normalising flow that transports a standard Gaussian prior $z_0 \sim \mathcal{N}(0, I)$ toward the empirical latent distribution $z_1 = \mu_\phi(x)$ via linear interpolation paths~\cite{lipman2023flow}: $z_t = (1 - t) z_0 + t z_1$. A two-layer MLP velocity network $v_\theta(z_t, t)$ is trained to predict the target velocity $z_1 - z_0$ at each interpolation time $t \in [0, 1]$ using the conditional flow matching loss:
\begin{equation}
    \mathcal{L}_{\mathrm{FM}} = \mathbb{E}_{t, z_0, z_1}\!\left[\| v_\theta(z_t, t) - (z_1 - z_0) \|_2^2\right].
    \label{eq:fm_loss}
\end{equation}
At inference, FM-refined latent codes are obtained by integrating $v_\theta$ from $t_{\mathrm{start}}$ to $t{=}1$ using Euler steps. Setting $t_{\mathrm{start}} < 1$ blends learned flow dynamics with the encoder output; $t_{\mathrm{start}} = 1$ recovers the original encoder latent (no FM). The FM step adds six base configurations as FM-augmented variants (e.g., VAE+FM, Full+FM), yielding 12 total configurations. Default settings: $t_{\mathrm{start}}{=}0.9$, FM epochs${=}200$, lr${=}10^{-3}$, hidden dim${=}128$, Euler steps${=}100$.

\subsection{Total Loss}
\label{sec:method:loss}

The total objective combines all active components~\eqref{eq:total_loss}:
\begin{align}
    \mathcal{L} ={} & \lambda_{\mathrm{recon}} \mathcal{L}_{\mathrm{recon}} + \lambda_{\mathrm{irecon}} \mathcal{L}_{\mathrm{irecon}} + \mathcal{L}_{\mathrm{ode}} + \mathcal{L}_{\mathrm{vel}} \nonumber \\
    & + \beta \cdot D_{\mathrm{KL}}(q_\phi(z|x) \| p(z)) \nonumber \\
    & + \lambda_{\mathrm{moco}} (\mathcal{L}_{\mathrm{moco}} + \mathcal{L}_{\mathrm{cross}}) + \lambda_{\mathrm{proto}} \mathcal{L}_{\mathrm{proto}},
    \label{eq:total_loss}
\end{align}
where each $\lambda$ is a configurable weight. The cross-path contrastive loss $\mathcal{L}_{\mathrm{cross}}$ aligns the ODE and VAE representations of the \emph{same cell} via symmetric NT-Xent~\eqref{eq:cross}:
\begin{equation}
    \mathcal{L}_{\mathrm{cross}} = \tfrac{1}{2}\!\left[\mathrm{CE}(\mathbf{S},\,\mathbf{I}) + \mathrm{CE}(\mathbf{S}^{\!\top}\!,\,\mathbf{I})\right],\;
    S_{ij} = \frac{\bar{h}^{\mathrm{vae}}_i \cdot \bar{h}^{\mathrm{ode}}_j}{\tau},
    \label{eq:cross}
\end{equation}
where $\bar{h}^{\mathrm{vae}} = \ell_2\text{-norm}(\mathrm{proj}(z))$, $\bar{h}^{\mathrm{ode}} = \ell_2\text{-norm}(\mathrm{proj}(z_{\mathrm{ode}}))$, $\mathbf{I}$ is the identity permutation (diagonal entries are positives), and CE denotes row-wise cross-entropy. The gradient of $\mathcal{L}_{\mathrm{cross}}$ flows only through the ODE path ($z$ is detached), so the ODE aligns to the encoder rather than vice versa.

%% ============================================================
\section{Experiments}
\label{sec:experiments}
%% ============================================================

\subsection{Datasets}
\label{sec:experiments:datasets}

We evaluate MoCoO on 20 scRNA-seq datasets spanning diverse biological contexts. Five core datasets are used for the main ablation study and downstream validation; an additional 15 datasets evaluate cross-dataset generalization and FM enhancement.

\textbf{Core datasets (5):}
\begin{enumerate}
    \item \textbf{IRALL (Hematopoiesis):} 41{,}252 cells, 12 types, 8 batches (d0--d30). Mouse bone marrow HSC-to-mature-lineage differentiation.
    \item \textbf{Dentate (Neurogenesis):} 18{,}213 cells, 14 types. Mouse dentate gyrus from radial glia to mature granule neurons.
    \item \textbf{Endo (Endocrine Pancreas):} 2{,}531 cells, 13 types. Mouse pancreatic ductal-to-endocrine differentiation.
    \item \textbf{Paul (Myeloid/Erythroid):} 2{,}730 cells, 19 types. Myeloid/erythroid bifurcation~\cite{paul2015transcriptional}; fine-grained with 19 clusters (some $< 30$ cells).
    \item \textbf{Spinoids (Spinal Cord Organoid):} 9{,}619 cells, 8 types. Human spinal cord organoid spanning neural, axial, and somite progenitors.
\end{enumerate}

\textbf{Extended datasets (15):} Astrocyte, Brain Metastasis, Breast, Gastric, Hemato, Hepatoblastoma, hESC-time, Liver Cancer, Lung, Melanoma, Pituitary, Retina, Setty, Spine, and Teeth. These cover additional human and mouse tissues and developmental systems, providing a comprehensive generalization benchmark. All 20 datasets are processed with identical hyperparameters (tuned on IRALL only) and evaluated using all 12 configurations (6 base + 6 FM), yielding $20 \times 12 = 240$ configuration--dataset evaluation points. For external benchmarking (Section~\ref{sec:discussion:baselines}), 7~core developmental datasets (IRALL, Paul, Endo, Dentate, Hemato, Lung, Spinoids) are evaluated across all 18~baselines on all five proposed metrics.

\subsection{Model Configurations}
\label{sec:experiments:configs}

Six configurations isolate the contribution of each component (Table~\ref{tab:configs}).

\begin{table}[!t]
    \centering
    \caption{Model configurations and their active components.}
    \label{tab:configs}
    \begin{tabular}{lcccc}
        \toprule
        Configuration   & VAE          & ODE          & MoCo         & Proto \\
        \midrule
        VAE             & $\checkmark$ &              &              &              \\
        VAE+ODE         & $\checkmark$ & $\checkmark$ &              &              \\
        VAE+MoCo        & $\checkmark$ &              & $\checkmark$ &              \\
        VAE+MoCo+Proto  & $\checkmark$ &              & $\checkmark$ & $\checkmark$ \\
        VAE+ODE+MoCo    & $\checkmark$ & $\checkmark$ & $\checkmark$ &              \\
        VAE+ODE+MoCo+Proto & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
        \bottomrule
    \end{tabular}
\end{table}

All models share: $d = 32$, $d_{\mathrm{ib}} = 4$, $h = 128$, NB likelihood, learning rate $10^{-4}$, batch size 128, 200 epochs (patience 40). MoCo settings: $K = 4096$, $m = 0.999$, $\tau = 0.2$, $\lambda_{\mathrm{moco}} = 0.3$ (with ODE) or $0.5$ (without), $P = 12$ prototypes, $\lambda_{\mathrm{proto}} = 0.1$. ODE settings: $\alpha_{\mathrm{vae}} = 0.6$, $\alpha_{\mathrm{ode}} = 0.4$; stop-gradient is applied to $z$ in ODE-specific losses (qz-divergence, velocity consistency, cross-path contrastive) to prevent the ODE from distorting encoder clusters. The KL weight $\beta$ is swept over $\{1.0, 0.1, 0.01\}$ to assess component sensitivity. Table~\ref{tab:hyperparams} lists all loss weights, their values, and provenance.

\begin{table}[!t]
    \centering
    \caption{Hyperparameter settings and provenance. The KL weight $\beta$ is systematically ablated ($\dagger$); other weights follow established defaults from the referenced methods.}
    \label{tab:hyperparams}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{llcl}
        \toprule
        Symbol & Description & Value & Source \\
        \midrule
        $\beta$$^\dagger$ & KL weight      & $\{0.01, 0.1, 1.0\}$ & Ablated (this work) \\
        $\lambda_{\mathrm{recon}}$ & Reconstruction       & 1.0          & Standard VAE \\
        $\lambda_{\mathrm{irecon}}$ & IB reconstruction   & 1.0          & Fixed \\
        $\lambda_{\mathrm{moco}}$ & MoCo contrastive      & 0.3 / 0.5   & MoCo~\cite{he2020momentum} \\
        $\lambda_{\mathrm{proto}}$ & Prototype contrastive & 0.1         & scGPCL~\cite{cai2024scgpcl} \\
        $\alpha_{\mathrm{vae}} / \alpha_{\mathrm{ode}}$ & Blend weights & 0.6 / 0.4 & Tuned (see text) \\
        $m$ & Momentum coefficient         & 0.999 & MoCo~\cite{he2020momentum} \\
        $\tau$ & Temperature                & 0.2   & MoCo~\cite{he2020momentum} \\
        $K$ & Queue size                    & 4096  & MoCo~\cite{he2020momentum} \\
        $P$ & Number of prototypes          & 12    & Matched to IRALL types \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

The blending weights $\alpha_{\mathrm{vae}} = 0.6$, $\alpha_{\mathrm{ode}} = 0.4$ were selected from $\alpha_{\mathrm{ode}} \in \{0.2, 0.3, 0.4, 0.5\}$ based on the ASW--DAV trade-off on IRALL; $\alpha_{\mathrm{ode}} > 0.5$ causes the ODE to dominate the latent space, degrading reconstruction, while $\alpha_{\mathrm{ode}} < 0.3$ under-utilises the temporal signal. The stop-gradient on $z$ in ODE-specific losses is essential: without it, the ODE gradient propagates through the encoder, collapsing cluster separation (ASW drops by ${\sim}0.04$ in preliminary experiments).

\subsection{Evaluation Metrics}
\label{sec:experiments:metrics}

We adopt five complementary metrics organised into two families. Because no single score captures all desiderata of a single-cell latent space, we combine standard clustering geometry measures with two novel composite embedding quality suites---\emph{DRE} and \emph{DREX}.

\textbf{Clustering geometry.} Average Silhouette Width (ASW$\uparrow$), Davies--Bouldin index (DAV$\downarrow$, lower is better), and Calinski--Harabasz index (CAL$\uparrow$). These three metrics capture complementary aspects of cluster quality: geometric separation between clusters (ASW), compactness-to-separation ratio (DAV), and the ratio of between-cluster to within-cluster dispersion (CAL).

\textbf{Dimensionality Reduction Evaluation (DRE).}
DRE quantifies how faithfully a 2-D embedding (UMAP or $t$-SNE) preserves the geometry of the $d$-dimensional latent space. Given $N$ cells with latent representations $\{z_i\}$ and 2-D coordinates $\{e_i\}$, we compute pairwise Euclidean distance matrices $D^{z}$ and $D^{e}$ and evaluate three complementary scores:
(i)~\emph{Distance correlation}---the Pearson correlation $r(D^{z}, D^{e})$ over all $\binom{N}{2}$ pairs, measuring global distance preservation;
(ii)~$Q_{\mathrm{local}}$---the fraction of each point's $k$-nearest neighbours ($k{=}15$) in $z$-space that remain among its $k$-nearest neighbours in 2-D, averaged over all points, capturing fine-grained local fidelity;
(iii)~$Q_{\mathrm{global}}$---the analogous neighbour-preservation fraction at $k{=}50$, capturing meso-scale structure.
The \emph{DRE overall quality} is the arithmetic mean of these three scores.

\textbf{Extended Dimensionality Reduction Evaluation (DREX).}
DREX supplements DRE with additional diagnostics:
(i)~\emph{Trustworthiness} and \emph{continuity} (following~\cite{venna2006local}), which respectively penalise false neighbours introduced in 2-D and true neighbours missing from 2-D;
(ii)~\emph{Distance Spearman} and \emph{Distance Pearson}---rank and linear correlations between $D^{z}$ and $D^{e}$;
(iii)~\emph{Local scale quality}---the Pearson correlation of each point's $k$-NN distances between $z$-space and 2-D, averaged over all points;
(iv)~\emph{Neighbourhood symmetry}---the mean bidirectional $k$-NN overlap, $\frac{1}{N}\sum_{i}\lvert\mathcal{N}^{z}_{k}(i)\cap\mathcal{N}^{e}_{k}(i)\rvert / k$;
(v)~\emph{$k$-NN rank correlation}---the per-point Spearman $\rho$ between neighbour ranks in $z$-space and 2-D, averaged over all cells.

\textit{Metric validation note:} DRE and DREX are composite metrics introduced in this work. Each individual sub-metric within these suites (trustworthiness, continuity, $k$-NN overlap) has an established basis in the manifold learning literature~\cite{venna2006local}. Together with the three clustering geometry metrics (ASW, DAV, CAL), these form our proposed five-metric evaluation suite that captures clustering geometry and embedding fidelity. Full computation code for all metrics is released with the package (\texttt{mocoo.evaluation}).

\textbf{Biological validation.} Cell-type purity, marker gene enrichment, and pseudotime--trajectory correlation (Spearman $\rho$).

\textbf{Batch integration (scIB).} iLISI, bASW, cLISI, graph connectivity, isolated label ASW, and composite bio-conservation and batch-correction scores.

\textbf{Computational cost.} Wall-clock training time and peak GPU memory.

\subsection{Evaluation Protocol}
\label{sec:experiments:protocol}

\textit{Data splits:} Each dataset is split into 70\% training, 15\% validation, and 15\% held-out test cells by stratified random sampling (preserving cell-type proportions). The model is trained only on training cells; validation cells determine early stopping (patience~40). All reported metrics, unless marked ``test,'' are computed on the \textit{validation} split. Validation and test metrics are compared to confirm generalisation.

\textit{KMeans cluster count:} $k$ is set to the ground-truth number of cell types, following Luecken~\textit{et~al.}~\cite{luecken2022scib}. This is standard practice for \textit{evaluation} of unsupervised methods---it provides fair cross-method comparison by removing the confounding effect of cluster-count selection. We additionally compute Leiden-based clustering~\cite{traag2019leiden} (resolution sweep, Section~\ref{sec:discussion:limitations}) as a reclustering-free alternative.

\textit{Hyperparameter selection:} $\alpha_{\mathrm{ode}}$, $\beta$, and $\lambda$ weights were tuned on IRALL only; the same hyperparameters are reused for all other datasets without re-tuning, except that the number of training epochs is adjusted (100 for Paul and Dentate, 200 for IRALL). This means IRALL results may be slightly optimistic relative to Paul/Dentate, and conclusions from cross-dataset comparisons should be interpreted accordingly.

%% ============================================================
%% ============================================================
\section{Results}
\label{sec:results}
%% ============================================================

We evaluate all six base configurations across 20 scRNA-seq datasets using the proposed five-metric evaluation suite (Fig.~\ref{fig:ablation_boxplots}). All hyperparameters were tuned on IRALL only and reused without re-tuning on other datasets; IRALL results may therefore be slightly optimistic (see Section~\ref{sec:experiments:protocol}). Cross-dataset mean performance is presented in Tables~\ref{tab:fm_clustering} and~\ref{tab:fm_embedding} (which include both base and FM-enhanced configurations). All genes are filtered to 3{,}000 HVGs. Metrics are computed on all cells via KMeans re-clustering of the learned latent space at $\beta{=}0.1$.

\subsection{Cross-Dataset Ablation}
\label{sec:results:crossdataset}

We evaluate two complementary metric families: embedding quality (DRE, DREX), which measures geometric fidelity of the learned representations, and clustering geometry (ASW, DAV, CAL), which captures discrete cell-type separability. A strong latent space should perform well across both families.

Table~\ref{tab:fm_embedding} presents embedding quality averaged across all 20 datasets for all 12 configurations. Among the six base configurations, ODE-containing variants consistently dominate: VAE+ODE achieves the best base DRE ($0.647$) and DREX ($0.613$), while VAE+ODE+MoCo ranks second on DRE ($0.640$). Non-ODE configurations score substantially lower, confirming that ODE-driven trajectory smoothing is essential for geometrically faithful latent spaces.

Table~\ref{tab:fm_clustering} presents clustering geometry. VAE+ODE+MoCo achieves the best base ASW ($0.225$) and DAV ($1.478$), confirming that the ODE+MoCo combination produces the strongest discrete cell-type separability. ODE-containing configurations consistently outperform non-ODE variants on ASW (${\sim}0.22$ vs.\ ${\sim}0.18$) and DAV (${\sim}1.49$ vs.\ ${\sim}1.72$).

Together, the ODE+MoCo combination achieves the strongest clustering geometry (best ASW, DAV) while remaining competitive on embedding quality (second-best DRE). Adding MoCo to VAE+ODE trades a small embedding quality reduction ($\Delta$DRE$=-0.007$, $\Delta$DREX$=-0.007$) for substantial cluster-separation gains ($\Delta$ASW$=+0.009$, $\Delta$DAV$=-0.026$). Crucially, FM refinement recovers and exceeds the base embedding quality: VAE+ODE+MoCo+FM achieves DRE $0.673$ and DREX $0.656$ (Table~\ref{tab:fm_embedding}), surpassing even the base VAE+ODE ($0.647$ and $0.613$), while retaining all clustering advantages. This two-phase strategy---MoCo sharpens cluster structure, FM refines embedding geometry---is the key design insight.

\textbf{MoCo+ODE synergy.}
Among the six base configurations, VAE+ODE+MoCo achieves the most top-two finishes (4/5): best ASW ($0.225$) and DAV ($1.478$) on the clustering side, plus second-best DRE ($0.640$) and DREX ($0.606$) on the embedding side (Tables~\ref{tab:fm_clustering}--\ref{tab:fm_embedding}). The synergy is clustering-led: MoCo sharpens cluster geometry beyond what ODE alone provides ($\Delta$ASW$=+0.009$, $\Delta$DAV$=-0.026$), while VAE+ODE retains the edge on raw embedding fidelity (best DRE $0.647$, DREX $0.613$). FM refinement resolves this trade-off in favour of ODE+MoCo (Section~\ref{sec:results:fm}). Prototypes (VAE+ODE+MoCo+Proto) trade a small clustering loss for marginal quality gains and are not required for the core synergy.

\textbf{FM amplification.}
When FM refinement is applied, the two ODE+MoCo variants dominate: VAE+ODE+MoCo+FM achieves the best ASW ($0.257$) and DAV ($1.359$), while Full+FM (with prototypes) achieves the best DRE ($0.678$), DREX ($0.660$), and CAL ($730.7$) (Tables~\ref{tab:fm_clustering}--\ref{tab:fm_embedding}). Both surpass even the base VAE+ODE on embedding quality (DRE $0.678$/$0.673$ vs.\ $0.647$; DREX $0.660$/$0.656$ vs.\ $0.613$), confirming that the ODE+MoCo clustering structure amplifies FM's geometric refinement ($\Delta$DREX$=+0.050$, $\Delta$DRE$=+0.033$ for VAE+ODE+MoCo; Table~\ref{tab:fm_delta}).

\subsection{Latent-Space Visualisation}
\label{sec:results:umap}

Per-dataset metric profiles across all 20 datasets and six base configurations reveal several qualitative patterns. First, ODE-containing configurations (VAE+ODE, VAE+ODE+MoCo, VAE+ODE+MoCo+Proto) consistently produce smoother cluster boundaries and more continuous developmental trajectories, consistent with their superior ASW and DAV scores in Table~\ref{tab:fm_clustering}. Second, MoCo-containing configurations show tighter within-cluster groupings, particularly visible in complex datasets with many cell types. Third, ODE+MoCo configurations combine both properties: smooth trajectories with well-defined cluster boundaries across all datasets, corroborating their superior performance across both metric families (Tables~\ref{tab:fm_clustering}--\ref{tab:fm_embedding}).

%% Metric profiles covered by Fig.~3 external benchmarking comparison.

\subsection{Biological Validation --- Pseudotime--Marker Correlations}
\label{sec:results:bio}

To validate that the ODE-derived pseudotime captures genuine developmental dynamics, we compute Spearman correlations between the learned pseudotime and canonical marker gene expression on all five datasets (VAE+ODE+MoCo+Proto, 200 epochs). \textit{Gene selection protocol:} For each dataset, we selected the top 6 genes by absolute Spearman $|\rho|$ with pseudotime from a predefined list of canonical lineage markers established in the original publications (e.g., \textit{Hbb-bs}/\textit{Cd34} for hematopoiesis~\cite{paul2015transcriptional}, \textit{Slc1a3}/\textit{Dcx} for neurogenesis, \textit{Ins2}/\textit{Neurog3} for endocrine pancreas). We did not cherry-pick genes post-hoc; all tested genes are reported. Per-dataset marker tables appear in Appendix~\ref{sec:supp:markers}; Table~\ref{tab:biovalidation} summarises the top correlation per dataset.

%% TABLE XIII -- Biovalidation Summary (kept in main body)
\begin{table}[!t]
    \centering
    \caption{Biovalidation summary across all five datasets (VAE+ODE+MoCo+Proto, 200 epochs).}
    \label{tab:biovalidation}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{llccl}
        \toprule
        Dataset  & Top Marker      & $\rho$   & $p$-value        & Biological Axis \\
        \midrule
        IRALL    & \textit{Hbb-bs} & $+0.275$ & $< 10^{-53}$     & HSC $\to$ erythroid/granulocyte \\
        Dentate  & \textit{Slc1a3} & $+0.542$ & $< 10^{-228}$    & RGC/stem $\to$ neuroblast \\
        Endo     & \textit{Ins2}   & $+0.463$ & $< 10^{-134}$    & Progenitor $\to$ $\beta$-cell \\
        Paul     & \textit{Epor}   & $+0.346$ & $< 10^{-77}$     & Progenitor $\to$ erythroid \\
        Spinoids & \textit{MKI67}  & $+0.492$ & $< 10^{-183}$    & Proliferating $\to$ differentiated \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

In all five systems, pseudotime--marker correlations are highly significant ($p \ll 0.001$) and biologically interpretable. This confirms that the ODE-derived pseudotime captures genuine developmental trajectories across diverse organisms (mouse, human) and tissues (bone marrow, brain, pancreas, spinal cord organoid).

\subsection{Pseudotime Validation Against Known Time-Points}
\label{sec:results:pseudotime}

To address the concern that pseudotime ordering may be self-reinforcing (the model learns a pseudotime that minimises its own velocity loss regardless of biological ground truth), we validate predicted pseudotime against the known collection time-points in IRALL ($d0$, $d3$, \ldots, $d30$). For each ODE-containing configuration trained at $\beta{=}0.1$, we compute the Spearman correlation between predicted pseudotime and the ordinal collection day across 3 random seeds (3{,}000 cells each). Table~\ref{tab:pseudotime_val} shows that the VAE+ODE+MoCo+Proto model achieves the strongest pseudotime--collection-day correlation among MoCoO configurations ($|\rho|{=}0.50{\pm}0.07$), followed by VAE+ODE+MoCo ($|\rho|{=}0.43{\pm}0.12$) and VAE+ODE ($|\rho|{=}0.18{\pm}0.12$). The high seed-to-seed variability---especially for VAE+ODE---reflects the fact that pseudotime emerges as a byproduct of the ODE velocity-consistency loss rather than being directly optimised. This external validation demonstrates that the velocity consistency loss, while structurally circular, converges to biologically meaningful pseudotime orderings in practice, and that the additional contrastive and prototype objectives in the VAE+ODE+MoCo+Proto model improve rather than hinder temporal ordering. The comparison highlights that MoCoO's pseudotime is a useful secondary output of a multi-objective representation learning framework rather than a replacement for dedicated trajectory inference methods.

\begin{table}[!t]
    \centering
    \caption{Pseudotime vs.\ collection day validation (IRALL, 3 seeds, $\beta{=}0.1$, 3{,}000 cells). $|\rho|$: mean $\pm$ std absolute Spearman correlation.}
    \label{tab:pseudotime_val}
    \begin{tabular}{lc}
        \toprule
        Method & $|\rho|$ \\
        \midrule
        VAE+ODE       & $0.18 \pm 0.12$ \\
        VAE+ODE+MoCo  & $0.43 \pm 0.12$ \\
        VAE+ODE+MoCo+Proto   & $\mathbf{0.50 \pm 0.07}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Training Dynamics}
\label{sec:results:training}

All configurations converge within 200 epochs (patience 40) at 3{,}000 cells. The VAE trains in ${\sim}27$\,s, MoCo-augmented models in ${\sim}35$\,s, and ODE-containing models in ${\sim}400$\,s (the ODE solver dominates wall-clock time). The computational overhead remains the primary cost of ODE integration (approximately 10--15$\times$ over the VAE baseline). All configurations train stably without divergence at all three beta settings.

\subsection{Flow Matching Enhancement}
\label{sec:results:fm}

A central claim of this work is that post-hoc FM refinement systematically improves latent-space quality across all model variants (Fig.~\ref{fig:ablation_boxplots}). To assess this, we apply FM to each of the six base configurations across all 20 datasets, yielding 12 total configurations and 120 base--FM comparison pairs (Table~\ref{tab:fm_delta}). FM consistently improves all five proposed metrics: DREX improves in 91.7\% of dataset--config pairs ($\Delta{=}+0.030 \pm 0.032$), DRE in 87.5\% ($\Delta{=}+0.023 \pm 0.028$), CAL in 88.3\%, ASW in 85.8\% ($\Delta{=}+0.018 \pm 0.034$), and DAV in 80.0\% ($\Delta{=}-0.072 \pm 0.132$, lower is better). The effect is largest for ODE-containing configurations, where FM amplifies the geometric structure already imposed by ODE-driven trajectory smoothing: VAE+ODE+MoCo gains $\Delta$DREX$=+0.050$ vs.\ $+0.012$ for VAE+MoCo without ODE. For non-ODE configurations, FM still provides small but consistent distance-preservation gains. Full per-config deltas are provided in Table~\ref{tab:fm_delta}.

\input{tables_fm}


We additionally compare MoCoO against 18~external baselines spanning classical ML, traditional single-cell tools, and deep learning autoencoders on 7~core developmental datasets (IRALL, Paul, Endo, Dentate, Hemato, Lung, Spinoids), evaluating all methods on all five proposed metrics (Fig.~\ref{fig:external_bench}). Downstream biological validation further extends beyond pseudotime to annotation transfer, uncertainty quantification, and generation quality (Fig.~\ref{fig:biovalidation}).

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{fig3_external_benchmarking}
    \caption{External benchmarking (\textbf{Fig.~3}). MoCoO (Full+FM) compared against 18~external baselines spanning classical ML (PCA, ICA, NMF, FA, TruncatedSVD), traditional single-cell tools (DPT, PCA+KMeans, Harmony), and deep learning autoencoders (scVI, scANVI, CellBLAST, CLEAR, GM-VAE, SCALEX, scDAC, scDeepCluster, scSMD, siVAE) on all five proposed metrics: ASW~$\uparrow$, DAV~$\downarrow$, CAL~$\uparrow$, DRE~$\uparrow$, DREX~$\uparrow$. All 19~methods are evaluated on all five metrics across 7~core developmental datasets (IRALL, Paul, Endo, Dentate, Hemato, Lung, Spinoids); Harmony is limited to the 3~datasets with batch annotations. CAL uses a logarithmic scale due to the wide dynamic range across methods. Each boxplot summarises per-dataset values; dashed lines separate method groups.}
    \label{fig:external_bench}
\end{figure*}

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{fig4_biovalidation}
    \caption{Biological validation (\textbf{Fig.~4}). \textbf{(a)}~Annotation transfer: kNN accuracy and macro-F1 across 5 developmental datasets (VAE+ODE+MoCo+Proto). Paul's 19 fine-grained subtypes explain the lower scores. \textbf{(b)}~Pseudotime--marker validation: top Spearman $|\rho|$ per dataset with gene annotations (all $p < 10^{-5}$). Pseudotime captures genuine developmental dynamics across diverse organisms and tissues. \textbf{(c)}~Generation quality: authenticity and normalised diversity of VAE-decoded synthetic cells.}
    \label{fig:biovalidation}
\end{figure*}

\subsection{Batch Integration}
\label{sec:results:batch}

We evaluate batch integration on the IRALL dataset (8 time-point batches, d0--d30) using the scIB benchmarking suite~\cite{luecken2022scib}. \textit{Caveat:} IRALL batches correspond to collection time-points (days~0--30), which reflect both technical batch effects and genuine biological progression. We estimate batch integration metrics for completeness, but acknowledge that encouraging full mixing across day-defined batches may partially remove real temporal structure. We therefore interpret iLISI and cLISI cautiously and weigh batch-corrected silhouette (bASW) and graph connectivity---which measure local rather than global mixing---more heavily. Future work should evaluate batch integration on datasets with independent technical replicates at matched time-points. Table~\ref{tab:batch_integration} reports all seven scIB metrics across configurations.

\begin{table}[!t]
    \centering
    \caption{Batch integration metrics (IRALL, 3{,}000 cells, $\beta{=}0.1$, scIB suite). Higher is better for all metrics. Best values in \textbf{bold}.}
    \label{tab:batch_integration}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lcccccc}
        \toprule
        Config & iLISI & bASW & cLISI & Graph & IsoASW & Overall \\
        \midrule
        VAE            & 0.069 & \textbf{0.872} & 0.920 & \textbf{0.974} & 0.794 & 0.641 \\
        VAE+ODE        & 0.125 & 0.848 & \textbf{0.933} & 0.953 & \textbf{0.808} & 0.651 \\
        VAE+MoCo       & 0.090 & 0.871 & 0.926 & 0.966 & 0.769 & 0.643 \\
        VAE+MoCo+Proto & 0.086 & 0.871 & 0.922 & 0.959 & 0.788 & 0.643 \\
        VAE+ODE+MoCo   & 0.131 & 0.853 & 0.929 & 0.939 & \textbf{0.808} & \textbf{0.652} \\
        \textbf{Full}  & \textbf{0.141} & 0.853 & 0.932 & 0.933 & 0.761 & 0.648 \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

ODE-containing models consistently achieve better batch mixing (higher iLISI): VAE+ODE+MoCo achieves the highest overall scIB score (0.652), followed by VAE+ODE (0.651) and VAE+ODE+MoCo+Proto (0.648). VAE+ODE+MoCo+Proto attains the highest iLISI (0.141), while VAE+ODE achieves the best cLISI (0.933), indicating strong batch mixing from ODE dynamics. The VAE baseline achieves the best bASW (0.872) and graph connectivity (0.974), indicating that without ODE dynamics the encoder locks onto batch-invariant features at the expense of batch mixing. The composite overall score (0.4$\times$bio + 0.6$\times$batch) favours configurations combining ODE and MoCo, confirming that multi-component architectures improve batch integration while preserving cell-type structure.

%% Downstream biological validation section removed from main body.
%% Full downstream analysis (annotation transfer, uncertainty, generation quality,
%% gene importance, branching, DE) is available in supplementary Fig.~S4.

%% ============================================================
%% ============================================================
\section{Discussion}
\label{sec:discussion}
%% ============================================================

\subsection{Component Contributions}
\label{sec:discussion:components}

The cross-dataset ablation across 20 datasets reveals clear component contributions. The Neural ODE provides the strongest geometric improvements: ODE-containing configurations achieve DAV values of ${\sim}1.49$ versus ${\sim}1.72$ for non-ODE variants, and ASW of ${\sim}0.22$ versus ${\sim}0.18$ (Table~\ref{tab:fm_clustering}). MoCo sharpens cluster separation when added to ODE ($\Delta$ASW$=+0.009$, $\Delta$DAV$=-0.026$) at a small embedding quality cost ($\Delta$DRE$=-0.007$, $\Delta$DREX$=-0.007$); this trade-off is favourable because FM refinement subsequently recovers and exceeds the base embedding quality (Section~\ref{sec:results:fm}). Together, VAE+ODE+MoCo achieves the most top-two finishes (4/5) among base configurations---both clustering (best ASW, DAV) and two in embedding (second-best DRE, DREX)---making it the strongest overall profile.

\subsection{Practical Configuration Guide}
\label{sec:discussion:guide}

Based on the cross-dataset results (20 datasets):
\begin{itemize}
    \item \textbf{Recommended default:} Use Full+FM (VAE+ODE+MoCo+Proto+FM) at $\beta{=}0.1$. This configuration achieves the best DRE ($0.678$), DREX ($0.660$), and CAL ($730.7$), with near-best ASW ($0.256$) and DAV ($1.375$). For maximum clustering geometry, VAE+ODE+MoCo+FM (without prototypes) achieves the best ASW ($0.257$) and DAV ($1.359$).
    \item \textbf{Without FM:} VAE+ODE+MoCo at $\beta{=}0.1$ is the strongest base model (best ASW $0.225$, DAV $1.478$; second-best DRE $0.640$, DREX $0.606$).
    \item \textbf{Maximising embedding quality:} VAE+ODE at $\beta{=}0.1$ achieves the best base DRE ($0.647$) and DREX ($0.613$).
    \item \textbf{Trajectory inference:} Use any ODE-containing configuration. The ODE-derived pseudotime correlates significantly with canonical markers across all five core datasets (Table~\ref{tab:biovalidation}).
    \item \textbf{Resource-constrained settings:} VAE+MoCo provides competitive clustering geometry (ASW $0.185$) without ODE computational overhead (${\sim}10\times$ speedup).
    \item \textbf{Prototypes (recommended):} Adding prototypes yields meaningful embedding quality gains with FM refinement (Full+FM achieves best DRE, DREX, CAL across all 12 configurations) at negligible computational cost.
\end{itemize}

\subsection{Component Contributions --- Summary}
\label{sec:discussion:summary}

Table~\ref{tab:component_summary} summarises each component's primary effect and best operating regime. The ODE+MoCo combination is the core synergy: VAE+ODE+MoCo ranks top-two on four of five proposed metrics among base configurations---both clustering (best ASW, DAV) and two in embedding quality (second-best DRE, DREX). MoCo sharpens cluster geometry at a small base embedding cost that FM refinement subsequently recovers: The two FM-enhanced ODE+MoCo variants collectively cover all five top-1 rankings: VAE+ODE+MoCo+FM leads on ASW and DAV, while Full+FM (with prototypes) leads on DRE, DREX, and CAL. This confirms that post-hoc geometric refinement is most effective when applied to an already well-structured latent space. Prototypes provide a meaningful embedding quality boost (Full+FM achieves $+0.005$ DRE and $+0.004$ DREX over VAE+ODE+MoCo+FM) and are recommended for the full pipeline.

\begin{table}[!t]
    \centering
    \caption{Component contributions summary.}
    \label{tab:component_summary}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{llcl}
        \toprule
        Component & Primary Effect & Best $\beta$ & Key Metrics Improved \\
        \midrule
        \textbf{ODE}          & Trajectory geometry       & All              & DAV, ASW, DRE \\
        \textbf{MoCo}         & Distance discrimination   & $\beta{\leq}0.1$ & DREX, ASW \\
        \textbf{ODE+MoCo}     & \textbf{Core synergy (recommended)} & $\beta{\leq}0.1$ & \textbf{ASW, DAV, DRE, DREX} \\
        \textbf{Proto}        & Embedding quality amplifier (with FM) & $\beta{\leq}0.1$    & DRE $+0.005$, DREX $+0.004$ \\
        \textbf{FM (Phase-2)} & \textbf{Embedding quality amplifier} & N/A & \textbf{DREX, DRE, ASW, DAV, CAL} \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

\subsection{FM Refinement Analysis}
\label{sec:discussion:fm}

The Phase-2 Flow Matching refinement acts as a post-hoc geometric regulariser. FM improves DREX in 91.7\% and DRE in 87.5\% of 120 dataset--configuration pairs (Table~\ref{tab:fm_delta}), with mean gains of $+0.030$ and $+0.023$, respectively. Notably, FM yields the largest absolute improvements for ODE-containing configurations: VAE+ODE+MoCo gains $\Delta$DREX$=+0.050$ vs.\ $+0.012$ for VAE+MoCo without ODE. This suggests that FM refinement amplifies the geometric structure already imposed by ODE-driven trajectory smoothing. For non-ODE configurations, FM still provides small but consistent distance-preservation gains ($\Delta$DRE$\geq+0.011$ for all base models).

\subsection{External Baseline Comparison}
\label{sec:discussion:baselines}

In comparison with 18~external baselines spanning classical ML, traditional single-cell tools, and deep learning autoencoders, MoCoO's recommended configuration (VAE+ODE+MoCo+Proto+FM) excels across all five proposed metrics (Fig.~\ref{fig:external_bench}). All methods---including all 8~deep learning baselines---are evaluated on all five metrics (ASW, DAV, CAL, DRE, DREX) across 7~core developmental datasets, ensuring a fair comparison. MoCoO achieves substantially higher ASW and lower (better) DAV than all external methods, with competitive CAL and strong embedding quality (DRE, DREX). Pseudotime validation against canonical marker genes (Fig.~\ref{fig:biovalidation}b) demonstrates that ODE-derived pseudotime captures genuine developmental dynamics across five organisms and tissues, with all top correlations highly significant ($p < 10^{-5}$). Additional downstream validation---including annotation transfer (Fig.~\ref{fig:biovalidation}a) and generation quality (Fig.~\ref{fig:biovalidation}c)---confirms the practical utility of MoCoO's latent space for diverse biological applications.

\subsection{Limitations}
\label{sec:discussion:limitations}

\begin{enumerate}
    \item \textbf{Subsample scale:} The ablation uses subsamples (2{,}500--3{,}000 cells per dataset). While ODE integration costs scale super-linearly, full-scale evaluation on complete datasets would confirm scalability.
    \item \textbf{KMeans evaluation:} All clustering metrics depend on KMeans re-clustering, which may penalise trajectory-shaped manifolds. Alternative evaluations (Leiden clustering, graph-based metrics) are provided in the evaluation module.
    \item \textbf{Hyperparameter count:} The total loss involves multiple tunable weights. We adopt established defaults from MoCo and VAE literature and systematically ablate $\beta$.
    \item \textbf{Prototype count:} $P{=}12$ is fixed across datasets regardless of cell-type count ($8$--$19$). Adaptive prototype selection remains an open question.
    \item \textbf{Velocity consistency circularity:} The velocity loss assumes cells ordered by pseudotime, yet pseudotime is model-derived. We validate against collection time-points and canonical markers (Tables~\ref{tab:pseudotime_val} and~\ref{tab:biovalidation}).
\end{enumerate}

%% ============================================================
\section{Conclusion}
\label{sec:conclusion}
%% ============================================================

MoCoO combines reconstruction fidelity (VAE), continuous dynamics (Neural ODE), and contrastive structure (MoCo) in a modular framework for single-cell representation learning, complemented by a systematic Flow Matching refinement step applicable to all model variants. Through systematic evaluation across six configurations (twelve with FM), 20 datasets, and a proposed five-metric evaluation suite, we demonstrate five key findings:

\textit{1. ODE+MoCo synergy.} Combining ODE and MoCo produces the best overall latent-space profile. VAE+ODE+MoCo achieves the highest cross-dataset mean ASW ($0.225$) and DAV ($1.478$), plus second-best DRE ($0.640$) and DREX ($0.606$)---four of five top-two finishes across 20 diverse datasets. MoCo sharpens cluster geometry beyond what ODE alone provides, at a small base embedding cost that FM refinement subsequently recovers.

\textit{2. FM refinement recovers and amplifies embedding quality.} The Phase-2 FM refinement step improves DREX in 91.7\% and DRE in 87.5\% of 120 dataset--configuration pairs, with mean improvements of $+0.030$ and $+0.023$ respectively. FM also improves clustering geometry: CAL in 88.3\%, ASW in 85.8\%, and DAV in 80.0\% of pairs. Applied to the ODE+MoCo synergy, VAE+ODE+MoCo+FM achieves the best ASW ($0.257$) and DAV ($1.359$), while the full pipeline with prototypes (Full+FM) achieves the best DRE ($0.678$), DREX ($0.660$), and CAL ($730.7$)---all surpassing the base VAE+ODE on embedding quality (DRE $0.678$ vs.\ $0.647$, DREX $0.660$ vs.\ $0.613$). FM is most effective when applied to an already well-structured latent space, with ODE-containing configurations gaining 5--8$\times$ more than non-ODE variants.

\textit{3. Cross-dataset robustness.} These advantages hold across 20 diverse scRNA-seq datasets spanning hematopoiesis, neurogenesis, pancreatic endocrine differentiation, cancer, organoid, and many additional developmental systems, demonstrating strong generalization without hyperparameter re-tuning.

\textit{4. Biological validation.} ODE-derived pseudotime correlates significantly with canonical developmental markers across all five core systems ($p \ll 0.001$), confirming biologically meaningful trajectory inference.

\textit{5. Multi-purpose latent space.} Unlike specialised external baselines, MoCoO provides clustering, trajectory inference, distance-preserving embeddings, batch integration, and downstream biological analysis (annotation transfer, uncertainty, generation, differential expression, branching detection) in a single unified framework.

For single-cell practitioners, we recommend the full MoCoO pipeline (VAE+ODE+MoCo+Proto+FM) at $\beta{\leq}0.1$ as the default configuration, which achieves the best embedding quality (DRE, DREX) and competitive clustering geometry; without FM, VAE+ODE+MoCo provides the strongest base model (see Section~\ref{sec:discussion:guide} for the full configuration guide). Future work includes scaling to atlas-level datasets ($>10^5$ cells), integration of multi-omic modalities, and extending FM refinement to conditional generation tasks. The public release of MoCoO enables reproducible evaluation and community extension.

\section*{Data Availability}
The 20 scRNA-seq datasets used in this study are publicly available. The five core datasets (IRALL, dentate gyrus, endocrine pancreas, Paul hematopoiesis, and spinoids) are accessible through the scvelo package~\cite{bergen2020generalizing} and original publications. The 15 extended datasets (astrocyte, brain metastasis, breast, gastric, hemato, hepatoblastoma, hESC-time, liver cancer, lung, melanoma, pituitary, retina, setty, spine, and teeth) are available from their respective original publications. All preprocessed datasets and trained model checkpoints will be made available at \url{https://github.com/PeterPonyu/MoCoO} upon publication.

\section*{Code Availability}
MoCoO is publicly available as an open-source Python package at \url{https://github.com/PeterPonyu/MoCoO} and installable via \texttt{pip install mocoo}. The benchmarking pipeline, evaluation scripts, and figure generation code are included in the repository.

\section*{Declaration of Competing Interest}
The author declares no competing interests.

\section*{Acknowledgment}
The author acknowledges the use of the University of Birmingham's BlueBEAR HPC service for computational resources. Experiments were conducted on a single NVIDIA RTX 4080 GPU with 12\,GB VRAM; training times range from ${\sim}27$\,s (VAE) to ${\sim}400$\,s (ODE-containing models) per dataset at 3{,}000 cells.

%% ============================================================
%% Supplementary Figures
%% ============================================================

\appendices

%% FM delta data available in Table~\ref{tab:fm_delta}; sensitivity data in supplementary.

\section{Metric Suite Rationale}
\label{sec:supp:subcategory}

Our proposed five-metric evaluation suite captures complementary aspects of single-cell latent-space quality. The three clustering geometry metrics (ASW, DAV, CAL) evaluate geometric separation, compactness-to-separation ratio, and between-to-within-cluster dispersion, respectively. The two embedding quality metrics (DRE, DREX) each aggregate multiple sub-scores. DRE measures faithfulness of 2-D UMAP projections through distance correlation, local neighbour preservation ($k{=}15$), and global neighbour preservation ($k{=}50$); DREX supplements this with rank-based and symmetry diagnostics including trustworthiness, continuity, neighbourhood symmetry, and $k$-NN rank correlation. Together, these five metrics provide a comprehensive evaluation without the redundancy of reporting 30+ individual sub-scores. Per-dataset breakdowns are available in the supplementary code repository.

\clearpage
\section{Pseudotime--Marker Correlation Tables}
\label{sec:supp:markers}

Tables~\ref{tab:irall_markers}--\ref{tab:spinoids_markers} present pseudotime--marker correlations for each dataset (VAE+ODE+MoCo+Proto, 200 epochs). See Section~\ref{sec:results:bio} for the gene selection protocol and Table~\ref{tab:biovalidation} for the cross-dataset summary.

\begin{table}[!t]
    \centering
    \caption{IRALL pseudotime--marker correlations (hematopoiesis).}
    \label{tab:irall_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Hbb-bs} & Erythroid hemoglobin     & $+0.275$ & $< 10^{-53}$ \\
        \textit{Hba-a1} & Erythroid hemoglobin     & $+0.264$ & $< 10^{-49}$ \\
        \textit{Elane}  & Granulocyte elastase     & $+0.255$ & $< 10^{-45}$ \\
        \textit{Cd34}   & HSC / progenitor         & $-0.233$ & $< 10^{-38}$ \\
        \textit{Ctsg}   & Granulocyte cathepsin G  & $+0.226$ & $< 10^{-36}$ \\
        \textit{Cd8a}   & T-cell marker            & $-0.168$ & $< 10^{-20}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Dentate pseudotime--marker correlations (neurogenesis).}
    \label{tab:dentate_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Slc1a3} & Astrocyte/RGC (GLAST)  & $+0.542$ & $< 10^{-228}$ \\
        \textit{Fabp7}  & Astrocyte/RGC (BLBP)   & $+0.476$ & $< 10^{-169}$ \\
        \textit{Sox2}   & Neural stem cell        & $+0.439$ & $< 10^{-141}$ \\
        \textit{Gfap}   & Astrocyte (GFAP)        & $+0.376$ & $< 10^{-101}$ \\
        \textit{Olig1}  & Oligodendrocyte         & $+0.282$ & $< 10^{-56}$  \\
        \textit{Dcx}    & Migrating neuroblast    & $-0.229$ & $< 10^{-37}$  \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Endo pseudotime--marker correlations (pancreatic endocrine).}
    \label{tab:endo_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Ins2}    & $\beta$-cell insulin        & $+0.463$ & $< 10^{-134}$ \\
        \textit{Ins1}    & $\beta$-cell insulin        & $+0.372$ & $< 10^{-83}$  \\
        \textit{Neurog3} & Endocrine progenitor        & $-0.319$ & $< 10^{-61}$  \\
        \textit{Pdx1}    & $\beta$-cell / progenitor TF & $+0.280$ & $< 10^{-47}$  \\
        \textit{Chgb}    & Pan-endocrine               & $+0.191$ & $< 10^{-22}$  \\
        \textit{Gcg}     & $\alpha$-cell glucagon      & $+0.181$ & $< 10^{-20}$  \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Paul pseudotime--marker correlations (myeloid/erythroid).}
    \label{tab:paul_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Epor}   & Erythropoietin receptor  & $+0.346$ & $< 10^{-77}$ \\
        \textit{Hba-a2} & Erythroid hemoglobin     & $+0.328$ & $< 10^{-69}$ \\
        \textit{Ly6c2}  & Myeloid surface marker   & $+0.272$ & $< 10^{-47}$ \\
        \textit{Elane}  & Granulocyte elastase     & $+0.253$ & $< 10^{-41}$ \\
        \textit{Gata1}  & Erythroid TF             & $+0.231$ & $< 10^{-34}$ \\
        \textit{Gata2}  & Stem/progenitor TF       & $-0.204$ & $< 10^{-27}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Spinoids pseudotime--marker correlations (spinal cord organoid).}
    \label{tab:spinoids_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{MKI67}   & Proliferation (Ki-67)         & $+0.492$ & $< 10^{-183}$ \\
        \textit{TOP2A}   & Proliferation (topoisomerase)  & $+0.438$ & $< 10^{-141}$ \\
        \textit{NES}     & Neural progenitor (nestin)     & $+0.198$ & $< 10^{-28}$  \\
        \textit{TUBB3}   & Neuronal $\beta$-tubulin       & $-0.154$ & $< 10^{-17}$  \\
        \textit{SOX2}    & Neural progenitor              & $+0.136$ & $< 10^{-14}$  \\
        \textit{NEUROG1} & Neurogenin-1                   & $-0.076$ & $< 10^{-5}$   \\
        \bottomrule
    \end{tabular}
\end{table}

%% ============================================================
%% References
%% ============================================================
\balance
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
