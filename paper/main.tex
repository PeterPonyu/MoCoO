%% MoCoO: Momentum Contrast ODE-Regularized VAE for Single-Cell
%% Trajectory Inference and Representation Learning
%%
%% Prepared for IEEE Journal of Biomedical and Health Informatics (J-BHI)
%% Using IEEEtran document class
%%
\documentclass[journal]{IEEEtran}

%% Packages
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{xspace}
\usepackage{bm}
\usepackage{balance}
\usepackage{tikz}

%% Graphics search path (generated figures live in benchmarks/figures/)
\graphicspath{{../benchmarks/figures/}{./}}

%% hyperref should be loaded last
\usepackage{hyperref}

%% Custom commands
\newcommand{\MoCoO}{\textsc{MoCoO}\xspace}
\newcommand{\ie}{\textit{i.e.}\xspace}
\newcommand{\eg}{\textit{e.g.}\xspace}

\begin{document}

\title{MoCoO: Momentum Contrast ODE-Regularized VAE for Single-Cell Trajectory Inference and Representation Learning}

\author{Zeyu~Fu%
\thanks{Manuscript submitted to IEEE Journal of Biomedical and Health Informatics.}%
\thanks{Z. Fu is with the School of Computer Science, University of Birmingham, Birmingham, B15 2TT, UK. (email: zxf123@student.bham.ac.uk)}
}

\markboth{IEEE Journal of Biomedical and Health Informatics}%
{Fu: MoCoO --- Momentum Contrast ODE-Regularized VAE for Single-Cell Trajectory Inference}

\maketitle

\begin{abstract}
Characterising cellular differentiation from single-cell RNA sequencing (scRNA-seq) requires representations that capture both discrete cell-type identity and continuous developmental trajectories. We present \MoCoO, a modular framework integrating a Variational Autoencoder (VAE), Neural Ordinary Differential Equations (Neural ODE), and Momentum Contrast (MoCo) with learnable prototypes. Through a systematic six-configuration ablation on three scRNA-seq datasets with multi-seed evaluation and external baselines (scVI, DPT, PCA+KMeans, Harmony, scANVI), we dissect each component's contribution across KL weight regimes ($\beta \in \{1.0, 0.1, 0.01\}$). Key findings: (1)~VAE+ODE is the dominant configuration, winning 13 of 27 metric--$\beta$ combinations while the Full model wins only 1---its advantages concentrate in distance-preservation metrics not captured by aggregate scores; (2)~MoCo is beta-dependent, effective only under weak KL regularisation ($\beta \leq 0.01$); (3)~ODE and MoCo show point estimates suggestive of synergy at low~$\beta$, with their combination improving cluster compactness and embedding fidelity beyond additive expectations, though confirmation with larger seed counts is needed. Pseudotime predictions correlate significantly with known collection time-points and canonical marker genes across five developmental systems. Leiden and KMeans clustering yield consistent rankings. Rather than claiming superiority of any single configuration, our findings characterise how temporal regularisation and contrastive learning interact in scRNA-seq latent spaces, providing practical guidelines for configuration selection. We publicly release the MoCoO Python package (\texttt{pip install mocoo}) and full benchmark suite.
\end{abstract}

\begin{IEEEkeywords}
Single-cell RNA sequencing, variational autoencoder, neural ordinary differential equations, momentum contrast, contrastive learning, trajectory inference, pseudotime, representation learning, computational biology.
\end{IEEEkeywords}

%% ============================================================
\section{Introduction}
\label{sec:introduction}
%% ============================================================

Single-cell RNA sequencing (scRNA-seq) has transformed our understanding of cellular heterogeneity by enabling transcriptome-wide profiling at single-cell resolution~\cite{tang2009mrna}. A fundamental analysis task is to learn low-dimensional representations that faithfully capture both \textit{discrete} cell-type identity and \textit{continuous} developmental dynamics such as differentiation trajectories~\cite{kiselev2019challenges,trapnell2014dynamics}. These representations underpin downstream tasks including clustering, pseudotime inference, RNA velocity estimation, and differential expression analysis~\cite{wolf2018scanpy}.

Variational Autoencoders (VAEs) have become the de facto standard for scRNA-seq dimensionality reduction, with methods such as scVI~\cite{lopez2018deep} and scVAE~\cite{gronbech2020scvae} demonstrating strong performance through count-based likelihood models (negative binomial, ZINB). However, standard VAE latent spaces tend to be over-smoothed, conflating nearby cell states. Two complementary strategies address this limitation.

First, Neural Ordinary Differential Equations (Neural ODEs)~\cite{chen2018neural} provide a principled framework for modelling continuous-time dynamics in latent space. By parameterising the latent derivative with a neural network and integrating via an ODE solver, these models learn smooth trajectories without discrete time-step assumptions. Latent ODE models~\cite{rubanova2019latent} have shown promise for time-series modelling, and recent work has adapted them to single-cell contexts for pseudotime inference and RNA velocity estimation~\cite{fu2025panode}.

Second, contrastive learning---particularly Momentum Contrast (MoCo)~\cite{he2020momentum}---learns representations that are locally smooth yet globally discriminative. MoCo uses a momentum-updated encoder and a large memory queue of negative keys, decoupling contrastive learning from batch size. Prototype contrastive learning~\cite{li2021prototypical} further imposes cluster structure by aligning representations with learnable prototype vectors.

No existing method unifies VAE reconstruction, Neural ODE dynamics, and momentum contrastive learning in a single framework. We propose \MoCoO (\textbf{Mo}mentum \textbf{Co}ntrast \textbf{O}DE-regularized VAE), a modular architecture that combines all three paradigms:

\begin{enumerate}
    \item \textbf{VAE with flexible count-based likelihoods} (MSE, NB, ZINB, Poisson, ZIP) provides a probabilistic latent space that handles zero-inflation and overdispersion in scRNA-seq counts.
    \item \textbf{Neural ODE regularisation} models continuous developmental dynamics, derives unsupervised pseudotime ordering, and produces gradient-based RNA velocity estimates.
    \item \textbf{Momentum Contrast with prototype heads} enforces instance-level discrimination via a momentum encoder and memory queue, with prototype learning that aligns representations to learnable cluster centres.
    \item \textbf{Information bottleneck} applies a secondary low-dimensional projection for hierarchical feature extraction.
\end{enumerate}
The framework additionally supports DIP-VAE, $\beta$-TC-VAE, and InfoVAE/MMD disentanglement regularisers, but these are not active in the experiments reported here.

We conduct a systematic ablation across six configurations (VAE, VAE+ODE, VAE+MoCo, VAE+MoCo+Proto, VAE+ODE+MoCo, and Full MoCoO) on five scRNA-seq datasets spanning diverse developmental systems. Multi-seed evaluation (5 seeds per configuration) provides statistical robustness. External baselines---scVI~\cite{lopez2018deep}, Diffusion Pseudotime (DPT)~\cite{haghverdi2016dpt}, PCA+KMeans, Harmony~\cite{korsunsky2019harmony}, and scANVI~\cite{xu2021probabilistic}---contextualise MoCoO's absolute performance. A counterintuitive finding is that the simplest ODE-augmented variant (VAE+ODE) dominates on aggregate metrics, winning 13 of 27 metric--$\beta$ combinations versus only 1 for the Full MoCoO. The Full model's value lies specifically in distance preservation---subcategory metrics masked by aggregate scores. We therefore frame our primary contribution as a systematic characterisation of how these components interact, rather than a claim that any single configuration is universally superior. Evaluation covers clustering (ARI, NMI, ASW, Calinski--Harabasz, Davies--Bouldin), dimensionality reduction quality (DRE, DREX, LSE, LSEX), biological validation (pseudotime--marker correlations), batch integration (iLISI, bASW, cLISI, graph connectivity, isolated label ASW via scIB~\cite{luecken2022scib}), training dynamics, and computational cost.

%% ============================================================
\section{Related Work}
\label{sec:related}
%% ============================================================

\subsection{Variational Autoencoders for scRNA-seq}
\label{sec:related:vae}

scVI~\cite{lopez2018deep} introduced the negative binomial VAE for scRNA-seq, jointly modelling library size and batch effects. Extensions include scVAE~\cite{gronbech2020scvae} (multiple count distributions), scANVI~\cite{xu2021probabilistic} (semi-supervised cell-type annotation), and totalVI~\cite{gayoso2021joint} (multi-omics). These methods focus on reconstruction quality and batch correction but do not explicitly model temporal dynamics.

\subsection{Neural ODEs and Latent ODE Models}
\label{sec:related:ode}

Neural ODEs~\cite{chen2018neural} parameterise continuous dynamical systems $\frac{dz}{dt} = f_\theta(z, t)$ and integrate using adaptive ODE solvers. The Latent ODE framework~\cite{rubanova2019latent} combines a VAE encoder with an ODE-governed latent space for irregularly sampled time series. Applications to single-cell biology include trajectory inference~\cite{fu2025panode}, while related dynamical modelling approaches have advanced RNA velocity estimation~\cite{bergen2020generalizing}. Optimal-transport-based methods such as TrajectoryNet~\cite{tong2020trajectorynet} and CellOT~\cite{bunne2022cellot} model population-level dynamics rather than single-cell ODE trajectories. MoCoO extends the PanODE framework~\cite{fu2025panode} (by the first author). Specifically, MoCoO inherits the VAE encoder--decoder, Neural ODE integration, and pseudotime-prediction head from PanODE. The novel contributions of the current work are: (i)~the MoCo module with momentum encoder and memory queue, (ii)~learnable prototype heads for cluster anchoring, (iii)~the information bottleneck, (iv)~the systematic six-configuration ablation design, and (v)~the DRE/DREX/LSE/LSEX evaluation suites. However, these methods typically lack contrastive regularisation, leading to latent spaces that may be geometrically faithful but poorly clustered.

\subsection{Contrastive Learning for Single-Cell Data}
\label{sec:related:contrastive}

MoCo~\cite{he2020momentum} and SimCLR~\cite{chen2020simple} have been adapted to biological contexts. scAGCL~\cite{cai2024scagcl} applies symmetric augmentation-guided contrastive learning to scRNA-seq data with cell-type-aware positive pair construction. scGPCL~\cite{cai2024scgpcl} introduces prototype contrastive learning that aligns cell embeddings with learnable prototype vectors representing cell types. These approaches improve clustering quality but do not incorporate temporal dynamics or ODE-based regularisation.

\subsection{Integrated Approaches}
\label{sec:related:integrated}

Several methods combine two of the three paradigms. scDiff~\cite{hao2024scdiff} uses diffusion models (related to score-based SDEs) for single-cell generation. VeloVAE~\cite{li2024velovae} integrates VAE with RNA velocity. Diffusion pseudotime (DPT)~\cite{haghverdi2016dpt} infers trajectories through diffusion maps of the $k$-NN graph, while Monocle~3~\cite{cao2020monocle3} and PAGA~\cite{wolf2019paga} use graph abstractions for trajectory inference, Slingshot~\cite{street2018slingshot} fits simultaneous principal curves for lineage-specific pseudotime, and Palantir~\cite{setty2019characterization} models cell-fate probabilities via diffusion maps for multiscale pseudotime estimation. For batch integration, Harmony~\cite{korsunsky2019harmony} applies iterative soft clustering in PCA space to remove batch effects. However, no prior work jointly combines VAE, Neural ODE, and momentum contrastive learning in a unified architecture for single-cell analysis.

%% ============================================================
\section{Method}
\label{sec:method}
%% ============================================================

\subsection{Overview}
\label{sec:method:overview}

MoCoO (Fig.~\ref{fig:architecture}) processes scRNA-seq count data $X \in \mathbb{R}^{N \times G}$ ($N$ cells, $G$ genes) through five components: (1)~a VAE with count-based decoder, (2)~a Neural ODE for continuous dynamics, (3)~a Momentum Contrast module with memory queue, (4)~an information bottleneck, and (5)~optional disentanglement regularisers.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{fig_architecture}
    \caption{\MoCoO architecture overview. Count data $X \in \mathbb{R}^{N \times G}$ passes through a VAE encoder to produce latent representations~$z$, optionally refined by a Neural ODE solver (orange) for continuous trajectory modelling. Momentum Contrast (green) with a memory queue ($K{=}4096$) enforces instance-level discrimination; learnable prototypes ($P{=}12$) impose cluster structure. An information bottleneck (purple) provides hierarchical compression to $d_{\mathrm{ib}}{=}4$. Dashed regions denote optional modules. Colour coding is consistent throughout all figures.}
    \label{fig:architecture}
\end{figure*}

\subsection{Encoder}
\label{sec:method:encoder}

The encoder $q_\phi(z|x)$ maps log-normalised expression $\tilde{x} = \log(1 + x)$ through a two-layer ReLU MLP to produce mean $\mu$ and pre-softplus scale $\sigma'$ of a diagonal Gaussian posterior~\eqref{eq:encoder}; latent samples are drawn via reparameterisation~\eqref{eq:reparam}:
\begin{equation}
    q_\phi(z|x) = \mathcal{N}\!\left(z;\, \mu_\phi(\tilde{x}),\, \mathrm{softplus}(\sigma'_\phi(\tilde{x}))^2 I\right),
    \label{eq:encoder}
\end{equation}
\begin{equation}
    z = \mu + \mathrm{softplus}(\sigma') \cdot \epsilon, \quad \epsilon \sim \mathcal{N}(0, I).
    \label{eq:reparam}
\end{equation}

When ODE mode is enabled, the encoder also predicts a scalar pseudotime $t \in [0, 1]$ via a sigmoid-activated linear head.

\subsection{Neural ODE Component}
\label{sec:method:ode}

Given latent samples $\{z_i\}$ and predicted pseudotimes $\{t_i\}$, the ODE module orders cells by pseudotime and solves~\eqref{eq:ode}:
\begin{equation}
    \frac{dz}{dt} = f_\psi(z, t), \quad z(t_0) = z_{\mathrm{init}},
    \label{eq:ode}
\end{equation}
where $f_\psi$ is a two-layer ELU MLP. Integration uses \texttt{torchdiffeq}'s fixed-step RK4 solver, chosen over adaptive methods (e.g., Dormand--Prince~\cite{dormand1980rk}) because the encoder-predicted pseudotime grid is relatively smooth and fixed-step integration provides deterministic gradients without adaptive step-size overhead---important for stable joint optimisation of the encoder and ODE function. Preliminary comparisons showed negligible ARI differences ($<0.01$) between RK4 and Dormand--Prince adaptive integration, with $\sim$30\% longer wall-clock time for the adaptive solver due to step-size adaptation overhead in the joint training loop. The ODE representations $z_{\mathrm{ode}}$ blend with VAE latent samples via configurable weights~\eqref{eq:blend}:
\begin{equation}
    z_{\mathrm{blend}} = \alpha_{\mathrm{vae}} \cdot z + \alpha_{\mathrm{ode}} \cdot z_{\mathrm{ode}}.
    \label{eq:blend}
\end{equation}

Two auxiliary losses regularise the ODE:
\begin{itemize}
    \item \textbf{ODE--VAE alignment:} $\mathcal{L}_{\mathrm{ode}} = \| z - z_{\mathrm{ode}} \|_2^2$
    \item \textbf{Velocity consistency:} $\mathcal{L}_{\mathrm{vel}} = 1 - \cos(z_{i+1} - z_i,\, f_\psi(z_i, t_i))$, aligning the learned velocity field with empirical displacements.
\end{itemize}

\subsection{Momentum Contrast Module}
\label{sec:method:moco}

The MoCo module follows MoCo~v1~\cite{he2020momentum} with enhancements from scAGCL~\cite{cai2024scagcl} and scGPCL~\cite{cai2024scgpcl}:

\begin{enumerate}
    \item \textbf{Query and Key Encoders:} The query encoder shares parameters with the VAE encoder; the key encoder is updated via EMA: $\theta_k \leftarrow m \cdot \theta_k + (1 - m) \cdot \theta_q$, $m = 0.999$.
    \item \textbf{Projection Heads:} Two-layer MLPs (BatchNorm, ReLU) map $d$-dimensional latent vectors to a $d_{\mathrm{proj}}$-dimensional contrastive space.
    \item \textbf{Memory Queue:} A FIFO buffer of size $K$ stores projected keys, decoupling the number of negatives from batch size.
    \item \textbf{InfoNCE Loss} (see~\eqref{eq:infonce}):
    \begin{equation}
        \mathcal{L}_{\mathrm{moco}} \!=\! -\log \frac{\exp(q \!\cdot\! k^+ / \tau)}{\exp(q \!\cdot\! k^+ / \tau) + \!\sum_{k^-} \exp(q \!\cdot\! k^- / \tau)},
        \label{eq:infonce}
    \end{equation}
    where the sum ranges over all $k^-$ in the memory queue and $\tau$ is the temperature.
    \item \textbf{Prototype Contrastive Loss} (optional; see~\eqref{eq:proto}):
    \begin{equation}
        \mathcal{L}_{\mathrm{proto}} = -\frac{1}{N} \sum_{i=1}^{N} \log \frac{\exp(z_i \cdot p_{c(i)} / \tau)}{\sum_{j=1}^{P} \exp(z_i \cdot p_j / \tau)},
        \label{eq:proto}
    \end{equation}
    where $\{p_j\}_{j=1}^{P}$ are learnable prototype vectors and $c(i) = \arg\max_j\, z_i \cdot p_j / \tau$ assigns cell $i$ to its nearest prototype.
\end{enumerate}

\textbf{Positive pair construction.} For each mini-batch, two stochastic augmented views of the same cell form positive pairs: the query view passes through the main encoder, while the key view passes through the momentum encoder. The augmentation pipeline applies three transformations, each with probability $p_{\mathrm{aug}}{=}0.5$: (1)~gene masking---randomly zeroing out genes with probability 0.1; (2)~Gaussian noise---adding $\mathcal{N}(0,\,0.04)$ to randomly selected genes (probability 0.1); (3)~feature swapping---exchanging gene values across cells in the batch (probability 0.05). Negatives come from all keys stored in the memory queue.

\textit{Augmentation rationale:} Gene masking simulates dropout, which is a dominant noise source in scRNA-seq protocols~\cite{hicks2018missing}. Gaussian noise ($\sigma{=}0.2$) models technical variability in library preparation. Feature swapping operates at very low probability ($p{=}0.05$, affecting ${\sim}5\%$ of genes per cell), which is equivalent to a regularised mixup---it encourages invariance to minor expression perturbations without creating biologically implausible hybrid profiles. All three augmentations are deliberately conservative: masking affects only 10\% of genes per view, noise is applied independently per gene, and swapping probability is below the natural inter-cell variance within a cell type. This design follows the principles validated in scAGCL~\cite{cai2024scagcl} and scGPCL~\cite{cai2024scgpcl}, which demonstrated that moderate stochastic augmentations improve contrastive learning for scRNA-seq without distorting cell-state semantics.

\subsection{Decoder}
\label{sec:method:decoder}

The decoder $p_\theta(x|z)$ reconstructs count data from the latent representation. It predicts normalised mean parameters $\rho = \mathrm{softmax}(\mathrm{MLP}(z))$ and uses gene-specific learnable dispersions $r$:
\begin{itemize}
    \item \textbf{Negative Binomial (NB):} $p(x_g | z) = \mathrm{NB}(x_g;\, \mu_g = l \cdot \rho_g,\, r_g)$
    \item \textbf{Zero-Inflated NB (ZINB):} $p(x_g | z) = \pi_g \cdot \delta_0(x_g) + (1 - \pi_g) \cdot \mathrm{NB}(x_g;\, \mu_g,\, r_g)$
\end{itemize}
where $l = \sum_g x_g$ is the library size and $\pi_g$ is a dropout probability predicted by a secondary decoder head. While MoCoO supports MSE, NB, ZINB, Poisson, and ZIP likelihoods, all experiments in this paper use the negative binomial (NB) likelihood unless otherwise stated.

\subsection{Information Bottleneck}
\label{sec:method:ib}

A secondary encoder-decoder pair maps $z \in \mathbb{R}^{d}$ to a bottleneck $b \in \mathbb{R}^{d_{\mathrm{ib}}}$ ($d_{\mathrm{ib}} = 4 < d$) and back to $\hat{z} \in \mathbb{R}^{d}$, imposing hierarchical compression that retains only the most salient features. The bottleneck reconstruction loss $\mathcal{L}_{\mathrm{irecon}} = \|\hat{z} - z\|_2^2$ is included in the total loss (Equation~\ref{eq:total_loss}) with weight $\lambda_{\mathrm{irecon}} = 1.0$. The IB serves as a fixed architectural component that forces the model to identify a low-dimensional summary of the latent representation; it is not ablated independently because preliminary experiments showed its effect to be minor compared to ODE and MoCo (ARI changes $< 0.01$), and it adds negligible computational cost.

\subsection{Total Loss}
\label{sec:method:loss}

The total objective combines all active components~\eqref{eq:total_loss}:
\begin{align}
    \mathcal{L} ={} & \lambda_{\mathrm{recon}} \mathcal{L}_{\mathrm{recon}} + \lambda_{\mathrm{irecon}} \mathcal{L}_{\mathrm{irecon}} + \mathcal{L}_{\mathrm{ode}} + \mathcal{L}_{\mathrm{vel}} \nonumber \\
    & + \beta \cdot D_{\mathrm{KL}}(q_\phi(z|x) \| p(z)) \nonumber \\
    & + \lambda_{\mathrm{moco}} (\mathcal{L}_{\mathrm{moco}} + \mathcal{L}_{\mathrm{cross}}) + \lambda_{\mathrm{proto}} \mathcal{L}_{\mathrm{proto}},
    \label{eq:total_loss}
\end{align}
where each $\lambda$ is a configurable weight. The cross-path contrastive loss $\mathcal{L}_{\mathrm{cross}}$ aligns the ODE and VAE representations of the \emph{same cell} via symmetric NT-Xent~\eqref{eq:cross}:
\begin{equation}
    \mathcal{L}_{\mathrm{cross}} = \tfrac{1}{2}\!\left[\mathrm{CE}(\mathbf{S},\,\mathbf{I}) + \mathrm{CE}(\mathbf{S}^{\!\top}\!,\,\mathbf{I})\right],\;
    S_{ij} = \frac{\bar{h}^{\mathrm{vae}}_i \cdot \bar{h}^{\mathrm{ode}}_j}{\tau},
    \label{eq:cross}
\end{equation}
where $\bar{h}^{\mathrm{vae}} = \ell_2\text{-norm}(\mathrm{proj}(z))$, $\bar{h}^{\mathrm{ode}} = \ell_2\text{-norm}(\mathrm{proj}(z_{\mathrm{ode}}))$, $\mathbf{I}$ is the identity permutation (diagonal entries are positives), and CE denotes row-wise cross-entropy. The gradient of $\mathcal{L}_{\mathrm{cross}}$ flows only through the ODE path ($z$ is detached), so the ODE aligns to the encoder rather than vice versa.

%% ============================================================
\section{Experiments}
\label{sec:experiments}
%% ============================================================

\subsection{Datasets}
\label{sec:experiments:datasets}

We evaluate MoCoO on five scRNA-seq datasets spanning diverse biological contexts.

\begin{enumerate}
    \item \textbf{IRALL (Hematopoiesis):} 41{,}252 cells, 12 types, 8 batches (d0--d30). Mouse bone marrow HSC-to-mature-lineage differentiation.
    \item \textbf{Dentate (Neurogenesis):} 18{,}213 cells, 14 types. Mouse dentate gyrus from radial glia to mature granule neurons.
    \item \textbf{Endo (Endocrine Pancreas):} 2{,}531 cells, 13 types. Mouse pancreatic ductal-to-endocrine differentiation.
    \item \textbf{Paul (Myeloid/Erythroid):} 2{,}730 cells, 19 types. Myeloid/erythroid bifurcation~\cite{paul2015transcriptional}; fine-grained with 19 clusters (some $< 30$ cells).
    \item \textbf{Spinoids (Spinal Cord Organoid):} 9{,}619 cells, 8 types. Human spinal cord organoid spanning neural, axial, and somite progenitors.
\end{enumerate}

\subsection{Model Configurations}
\label{sec:experiments:configs}

Six configurations isolate the contribution of each component (Table~\ref{tab:configs}).

\begin{table}[!t]
    \centering
    \caption{Model configurations and their active components.}
    \label{tab:configs}
    \begin{tabular}{lcccc}
        \toprule
        Configuration   & VAE          & ODE          & MoCo         & Proto \\
        \midrule
        VAE             & $\checkmark$ &              &              &              \\
        VAE+ODE         & $\checkmark$ & $\checkmark$ &              &              \\
        VAE+MoCo        & $\checkmark$ &              & $\checkmark$ &              \\
        VAE+MoCo+Proto  & $\checkmark$ &              & $\checkmark$ & $\checkmark$ \\
        VAE+ODE+MoCo    & $\checkmark$ & $\checkmark$ & $\checkmark$ &              \\
        Full (MoCoO)    & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
        \bottomrule
    \end{tabular}
\end{table}

All models share: $d = 32$, $d_{\mathrm{ib}} = 4$, $h = 128$, NB likelihood, learning rate $10^{-4}$, batch size 128, 200 epochs (patience 40). MoCo settings: $K = 4096$, $m = 0.999$, $\tau = 0.2$, $\lambda_{\mathrm{moco}} = 0.3$ (with ODE) or $0.5$ (without), $P = 12$ prototypes, $\lambda_{\mathrm{proto}} = 0.1$. ODE settings: $\alpha_{\mathrm{vae}} = 0.6$, $\alpha_{\mathrm{ode}} = 0.4$; stop-gradient is applied to $z$ in ODE-specific losses (qz-divergence, velocity consistency, cross-path contrastive) to prevent the ODE from distorting encoder clusters. The KL weight $\beta$ is swept over $\{1.0, 0.1, 0.01\}$ to assess component sensitivity. Table~\ref{tab:hyperparams} lists all loss weights, their values, and provenance.

\begin{table}[!t]
    \centering
    \caption{Hyperparameter settings and provenance. The KL weight $\beta$ is systematically ablated ($\dagger$); other weights follow established defaults from the referenced methods.}
    \label{tab:hyperparams}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{llcl}
        \toprule
        Symbol & Description & Value & Source \\
        \midrule
        $\beta$$^\dagger$ & KL weight      & $\{0.01, 0.1, 1.0\}$ & Ablated (this work) \\
        $\lambda_{\mathrm{recon}}$ & Reconstruction       & 1.0          & Standard VAE \\
        $\lambda_{\mathrm{irecon}}$ & IB reconstruction   & 1.0          & Fixed \\
        $\lambda_{\mathrm{moco}}$ & MoCo contrastive      & 0.3 / 0.5   & MoCo~\cite{he2020momentum} \\
        $\lambda_{\mathrm{proto}}$ & Prototype contrastive & 0.1         & scGPCL~\cite{cai2024scgpcl} \\
        $\alpha_{\mathrm{vae}} / \alpha_{\mathrm{ode}}$ & Blend weights & 0.6 / 0.4 & Tuned (see text) \\
        $m$ & Momentum coefficient         & 0.999 & MoCo~\cite{he2020momentum} \\
        $\tau$ & Temperature                & 0.2   & MoCo~\cite{he2020momentum} \\
        $K$ & Queue size                    & 4096  & MoCo~\cite{he2020momentum} \\
        $P$ & Number of prototypes          & 12    & Matched to IRALL types \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

The blending weights $\alpha_{\mathrm{vae}} = 0.6$, $\alpha_{\mathrm{ode}} = 0.4$ were selected from $\alpha_{\mathrm{ode}} \in \{0.2, 0.3, 0.4, 0.5\}$ based on the ARI--DB trade-off on IRALL; $\alpha_{\mathrm{ode}} > 0.5$ causes the ODE to dominate the latent space, degrading reconstruction, while $\alpha_{\mathrm{ode}} < 0.3$ under-utilises the temporal signal. The stop-gradient on $z$ in ODE-specific losses is essential: without it, the ODE gradient propagates through the encoder, collapsing cluster separation (ARI drops by ${\sim}0.08$ in preliminary experiments).

\subsection{Evaluation Metrics}
\label{sec:experiments:metrics}

We adopt six complementary metric families. Because no single score captures all desiderata of a single-cell latent space, we introduce two novel composite suites---\emph{DRE/DREX} and \emph{LSE/LSEX}---alongside standard clustering, biological, and integration benchmarks.

\textbf{Clustering quality.} Adjusted Rand Index (ARI), Normalized Mutual Information (NMI), Average Silhouette Width (ASW), Calinski--Harabasz index (CH), and Davies--Bouldin index (DB$\downarrow$).

\textbf{Dimensionality Reduction Evaluation (DRE).}
DRE quantifies how faithfully a 2-D embedding (UMAP or $t$-SNE) preserves the geometry of the $d$-dimensional latent space. Given $N$ cells with latent representations $\{z_i\}$ and 2-D coordinates $\{e_i\}$, we compute pairwise Euclidean distance matrices $D^{z}$ and $D^{e}$ and evaluate three complementary scores:
(i)~\emph{Distance correlation}---the Pearson correlation $r(D^{z}, D^{e})$ over all $\binom{N}{2}$ pairs, measuring global distance preservation;
(ii)~$Q_{\mathrm{local}}$---the fraction of each point's $k$-nearest neighbours ($k{=}15$) in $z$-space that remain among its $k$-nearest neighbours in 2-D, averaged over all points, capturing fine-grained local fidelity;
(iii)~$Q_{\mathrm{global}}$---the analogous neighbour-preservation fraction at $k{=}50$, capturing meso-scale structure.
The \emph{DRE overall quality} is the arithmetic mean of these three scores.

\textbf{Extended Dimensionality Reduction Evaluation (DREX).}
DREX supplements DRE with additional diagnostics:
(i)~\emph{Trustworthiness} and \emph{continuity} (following~\cite{venna2006local}), which respectively penalise false neighbours introduced in 2-D and true neighbours missing from 2-D;
(ii)~\emph{Distance Spearman} and \emph{Distance Pearson}---rank and linear correlations between $D^{z}$ and $D^{e}$;
(iii)~\emph{Local scale quality}---the Pearson correlation of each point's $k$-NN distances between $z$-space and 2-D, averaged over all points;
(iv)~\emph{Neighbourhood symmetry}---the mean bidirectional $k$-NN overlap, $\frac{1}{N}\sum_{i}\lvert\mathcal{N}^{z}_{k}(i)\cap\mathcal{N}^{e}_{k}(i)\rvert / k$;
(v)~\emph{$k$-NN rank correlation}---the per-point Spearman $\rho$ between neighbour ranks in $z$-space and 2-D, averaged over all cells.

\textbf{Latent Structure Evaluation (LSE/LSEX).}
LSE assesses the intrinsic quality of the latent space without reference to any downstream clustering or embedding, using SVD-based measures including manifold dimensionality (participation ratio), spectral decay rate, and anisotropy score. LSEX adds label-aware diagnostics: cluster compactness, inter-cluster gap, $k$-NN purity, and sampling stability. Full metric definitions appear in Appendix~\ref{sec:supp:metric_defs}.

\textit{Metric validation note:} DRE/DREX and LSE/LSEX are composite metrics introduced in this work. Each individual sub-metric within these suites (trustworthiness, continuity, $k$-NN overlap, participation ratio, spectral decay) has an established basis in the manifold learning literature~\cite{venna2006local}. To verify that our main conclusions are robust to metric choice, we confirm that configuration rankings based on the five standard clustering metrics alone (ARI, NMI, ASW, CH, DB) yield the same qualitative findings: ODE is the dominant component, MoCo is $\beta$-dependent, and VAE+ODE wins the majority of metric--$\beta$ comparisons (Tables~\ref{tab:beta1}--\ref{tab:beta001}). The DRE/DREX/LSE/LSEX suites provide additional geometric insight but do not drive any conclusion that contradicts the standard metrics. Full computation code for all metrics is released with the package (\texttt{mocoo.evaluation}).

\textbf{Biological validation.} Cell-type purity, marker gene enrichment, and pseudotime--trajectory correlation (Spearman $\rho$).

\textbf{Batch integration (scIB).} iLISI, bASW, cLISI, graph connectivity, isolated label ASW, and composite bio-conservation and batch-correction scores.

\textbf{Computational cost.} Wall-clock training time and peak GPU memory.

\subsection{Evaluation Protocol}
\label{sec:experiments:protocol}

\textit{Data splits:} Each dataset is split into 70\% training, 15\% validation, and 15\% held-out test cells by stratified random sampling (preserving cell-type proportions). The model is trained only on training cells; validation cells determine early stopping (patience~40). All reported metrics, unless marked ``test,'' are computed on the \textit{validation} split. Figures~\ref{fig:quant_comparison} and~\ref{fig:generalization} explicitly compare validation and test metrics to confirm generalisation.

\textit{KMeans cluster count:} $k$ is set to the ground-truth number of cell types, following Luecken~\textit{et~al.}~\cite{luecken2022scib}. This is standard practice for \textit{evaluation} of unsupervised methods---it provides fair cross-method comparison by removing the confounding effect of cluster-count selection. We additionally compute Leiden-based clustering (resolution sweep, Section~\ref{sec:discussion:limitations}) as a reclustering-free alternative.

\textit{Hyperparameter selection:} $\alpha_{\mathrm{ode}}$, $\beta$, and $\lambda$ weights were tuned on IRALL only; the same hyperparameters are reused for all other datasets without re-tuning, except that the number of training epochs is adjusted (100 for Paul and Dentate, 200 for IRALL). This means IRALL results may be slightly optimistic relative to Paul/Dentate, and conclusions from cross-dataset comparisons should be interpreted accordingly.

%% ============================================================
\section{Results}
\label{sec:results}
%% ============================================================

We evaluate all six configurations on the IRALL hematopoiesis dataset. \textit{Note:} All hyperparameters ($\alpha_{\mathrm{ode}}$, $\lambda$ weights) were tuned on IRALL only and reused without re-tuning on other datasets; IRALL results may therefore be slightly optimistic (see Section~\ref{sec:experiments:protocol}). \textit{Beta sweep} (Tables~\ref{tab:beta1}--\ref{tab:beta001}): 3{,}000 cells, 200 epochs (patience 40), $\beta \in \{1.0, 0.1, 0.01\}$, targeting component interaction analysis. \textit{Multi-seed evaluation} (Table~\ref{tab:multiseed}): 5 random seeds per configuration with mean $\pm$ standard deviation, confirming statistical robustness. \textit{External baselines} (Table~\ref{tab:external}): scVI, DPT, PCA+KMeans, Harmony, and scANVI under identical preprocessing. \textit{Full-scale evaluation} (Figs.~\ref{fig:quant_comparison},~\ref{fig:training_dynamics},~\ref{fig:composed_benchmark}): same setting with $\beta = 0.1$, providing UMAP visualisation, training dynamics, and comprehensive metric comparison. All genes are filtered to 3{,}000 HVGs. Metrics are computed on all cells via KMeans re-clustering of the learned latent space.

\subsection{Quantitative Comparison --- Beta Sweep}
\label{sec:results:quant}

Tables~\ref{tab:beta1}--\ref{tab:beta001} present the full quantitative comparison across all six configurations at each KL weight setting (3{,}000 cells, 200 epochs). Fig.~\ref{fig:quant_comparison} provides a complementary visual comparison at $\beta = 0.1$.

%% TABLE I
\begin{table*}[!t]
    \centering
    \caption{IRALL, 3{,}000 cells, 200 epochs --- $\beta = 1.0$ (standard KL). Best values in \textbf{bold}. DB$\downarrow$ indicates lower is better.}
    \label{tab:beta1}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lccccccccc}
        \toprule
        Config & ARI & NMI & ASW & CH & DB$\downarrow$ & LSE & DRE & DREX & LSEX \\
        \midrule
        VAE              & 0.395          & 0.543          & 0.122          & 203.4          & 2.139          & 0.317          & 0.415          & 0.591          & 0.608          \\
        \textbf{VAE+ODE} & \textbf{0.485} & 0.558          & \textbf{0.212} & \textbf{401.4} & \textbf{1.522} & 0.208          & \textbf{0.454} & \textbf{0.618} & \textbf{0.610} \\
        VAE+MoCo         & 0.374          & 0.513          & 0.130          & 204.8          & 2.054          & 0.315          & 0.421          & 0.587          & 0.608          \\
        VAE+MoCo+Proto   & 0.422          & 0.528          & 0.140          & 202.4          & 2.025          & \textbf{0.319} & 0.402          & 0.574          & 0.609          \\
        VAE+ODE+MoCo     & 0.480          & \textbf{0.574} & 0.192          & 389.9          & 1.647          & 0.204          & 0.444          & 0.586          & 0.608          \\
        Full (MoCoO)     & 0.396          & 0.537          & 0.173          & 379.4          & 1.714          & 0.204          & 0.452          & 0.610          & 0.609          \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

At strong KL ($\beta = 1.0$), ODE is the dominant component: VAE+ODE achieves the best ARI (0.485), ASW (0.212), CH (401.4), DB (1.522), DRE (0.454), DREX (0.618), and LSEX (0.610)---winning 7 of 9 metrics. VAE+MoCo+Proto wins LSE (0.319), indicating that prototypes contribute to spectral quality. VAE+ODE+MoCo achieves the best NMI (0.574), indicating that contrastive learning adds complementary information-theoretic structure.

%% TABLE II
\begin{table*}[!t]
    \centering
    \caption{IRALL, 3{,}000 cells, 200 epochs --- $\beta = 0.1$ (moderate KL). Best values in \textbf{bold}. DB$\downarrow$ indicates lower is better.}
    \label{tab:beta01}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lccccccccc}
        \toprule
        Config & ARI & NMI & ASW & CH & DB$\downarrow$ & LSE & DRE & DREX & LSEX \\
        \midrule
        VAE              & \textbf{0.489} & 0.561          & 0.189          & 371.0          & 1.675          & 0.228          & 0.494          & 0.650          & 0.593          \\
        \textbf{VAE+ODE} & 0.475          & \textbf{0.573} & 0.292          & \textbf{959.4} & \textbf{1.168} & 0.138          & \textbf{0.547} & \textbf{0.689} & 0.588          \\
        VAE+MoCo         & 0.470          & 0.554          & 0.188          & 360.2          & 1.700          & 0.232          & 0.500          & 0.656          & 0.593          \\
        VAE+MoCo+Proto   & 0.431          & 0.563          & 0.157          & 355.6          & 1.813          & \textbf{0.239} & 0.515          & 0.664          & \textbf{0.594} \\
        VAE+ODE+MoCo     & 0.473          & 0.561          & \textbf{0.307} & 680.6          & 1.190          & 0.174          & 0.529          & 0.666          & 0.591          \\
        Full (MoCoO)     & 0.446          & 0.559          & 0.272          & 697.3          & 1.185          & 0.152          & 0.518          & 0.678          & 0.589          \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

At moderate KL ($\beta = 0.1$), ODE remains the strongest single component: VAE+ODE wins NMI (0.573), CH (959.4), DB (1.168), DRE (0.547), and DREX (0.689). The VAE leads ARI (0.489), and VAE+ODE+MoCo achieves the best ASW (0.307). VAE+MoCo+Proto wins LSE (0.239) and LSEX (0.594), suggesting prototypes contribute to latent structure quality at moderate regularisation.

%% TABLE III
\begin{table*}[!t]
    \centering
    \caption{IRALL, 3{,}000 cells, 200 epochs --- $\beta = 0.01$ (weak KL). Best values in \textbf{bold}. DB$\downarrow$ indicates lower is better.}
    \label{tab:beta001}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lccccccccc}
        \toprule
        Config & ARI & NMI & ASW & CH & DB$\downarrow$ & LSE & DRE & DREX & LSEX \\
        \midrule
        VAE              & 0.441          & 0.570          & 0.199          & 485.4          & 1.557          & 0.188          & 0.526          & 0.680          & 0.595          \\
        VAE+ODE          & 0.465          & 0.573          & \textbf{0.285} & 621.8          & 1.278          & 0.173          & 0.488          & 0.643          & 0.588          \\
        \textbf{VAE+MoCo}& \textbf{0.480} & 0.579          & 0.188          & 431.8          & 1.630          & 0.203          & 0.535          & \textbf{0.686} & \textbf{0.596} \\
        VAE+MoCo+Proto   & 0.420          & 0.566          & 0.162          & 437.6          & 1.748          & \textbf{0.205} & \textbf{0.541} & 0.685          & \textbf{0.596} \\
        VAE+ODE+MoCo     & 0.477          & 0.570          & 0.259          & \textbf{719.0} & \textbf{1.222} & 0.163          & 0.528          & 0.679          & 0.590          \\
        Full (MoCoO)     & 0.474          & \textbf{0.581} & 0.269          & 629.3          & 1.240          & 0.171          & 0.518          & 0.669          & 0.589          \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

At weak KL ($\beta = 0.01$), the competition broadens: VAE+MoCo leads ARI (0.480) and DREX (0.686); VAE+MoCo and VAE+MoCo+Proto tie for LSEX (0.596); VAE+MoCo+Proto wins LSE (0.205) and DRE (0.541); VAE+ODE+MoCo wins CH (719.0) and DB (1.222); Full achieves the best NMI (0.581). This regime allows the most component diversity, with wins distributed across four different configurations.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.42\textheight,keepaspectratio]{fig2_quant_comparison}
    \caption{Latent-space comparison across six configurations (IRALL, 3{,}000 cells, 200 epochs, $\beta{=}0.1$). \textbf{(A)}~UMAP projections coloured by cell type. \textbf{(B)}~Clustering metrics (ARI, NMI, ASW); solid bars = validation, hatched = held-out test. \textbf{(C)}~Neighbourhood-preservation metrics (trustworthiness, neighbour symmetry, distance Spearman~$\rho$, DRE). \textbf{(D)}~Intrinsic latent-space quality (participation ratio, manifold dimensionality, anisotropy, composite LSE). Bold outlines mark the best value per metric.}
    \label{fig:quant_comparison}
\end{figure*}

\subsection{Component Effect Analysis}
\label{sec:results:component}

We isolate each component's marginal effect by computing deltas between adjacent configurations across all three beta settings (Table~\ref{tab:component_effects}; see also Fig.~\ref{fig:ablation_summary} for a visual summary).

%% TABLE IV
\begin{table*}[!t]
    \centering
    \caption{Component effects ($\Delta$ from baseline, by $\beta$). Bold values indicate the most notable effects.}
    \label{tab:component_effects}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llrrrl}
        \toprule
        Component & Metric & $\beta{=}1.0$ & $\beta{=}0.1$ & $\beta{=}0.01$ & Interpretation \\
        \midrule
        \textbf{ODE} (VAE$\to$VAE+ODE)     & $\Delta$ARI  & $\mathbf{+0.090}$ & $-0.014$ & $+0.024$ & Strong ARI boost at $\beta{=}1.0$ \\
                                             & $\Delta$DB   & $\mathbf{-0.617}$ & $\mathbf{-0.507}$ & $-0.279$ & Consistently improves DB (better) \\
                                             & $\Delta$LSEX & $+0.002$ & $-0.005$ & $-0.007$ & Marginal LSEX effect \\
        \midrule
        \textbf{MoCo} (VAE$\to$VAE+MoCo)    & $\Delta$ARI  & $-0.021$ & $-0.019$ & $\mathbf{+0.039}$ & \textbf{Beta-dependent}: effective at $\beta{=}0.01$ \\
                                             & $\Delta$ASW  & $+0.008$ & $-0.001$ & $-0.011$ & Mixed \\
                                             & $\Delta$DREX & $-0.004$ & $+0.006$ & $+0.006$ & Small positive DREX at low $\beta$ \\
        \midrule
        \textbf{Proto} (MoCo$\to$MoCo+P)    & $\Delta$ARI  & $\mathbf{+0.048}$ & $-0.039$ & $-0.060$ & Helps ARI at $\beta{=}1.0$ only \\
                                             & $\Delta$DB   & $-0.029$ & $+0.113$ & $+0.118$ & Worsens DB at low $\beta$ \\
                                             & $\Delta$DRE  & $-0.019$ & $+0.015$ & $+0.006$ & Small DRE boost at $\beta{\le}0.1$ \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

ODE is the dominant component at full training scale, consistently improving DB (cluster compactness) by $0.28$--$0.62$ across all beta values. Its ARI boost is strongest at $\beta=1.0$ ($+0.090$), where temporal regularisation compensates for the strong prior constraint. MoCo's ARI contribution is beta-dependent: effective at $\beta=0.01$ ($\Delta$ARI$=+0.039$) but marginal otherwise. Prototypes boost ARI only at $\beta=1.0$ ($+0.048$).

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.42\textheight,keepaspectratio]{fig3_ablation_summary}
    \caption{Component contribution analysis (IRALL, $\beta{=}0.1$). \textbf{(A)}~Two-part summary panel: \textbf{A1} ODE$\times$MoCo interaction heatmap across metrics and $\beta$ values, and \textbf{A2} normalized metric profiles across configurations. \textbf{(B)}~Incremental-gain waterfall for ARI, NMI, and ASW: each bar shows the marginal $\Delta$ from the VAE baseline. \textbf{(C)}~Column-normalised performance heatmap across eight metrics. \textbf{(D)}~Permutation-based kNN accuracy drop per latent dimension ($d{=}32$), showing representation concentration per model.}
    \label{fig:ablation_summary}
\end{figure*}

\subsection{ODE \texorpdfstring{$\times$}{x} MoCo Synergy Analysis}
\label{sec:results:synergy}

To test whether ODE and MoCo interact synergistically (beyond additive effects), we compute the interaction term in~\eqref{eq:synergy}:
\begin{multline}
    \Delta_{\mathrm{synergy}} = \mathrm{(VAE{+}ODE{+}MoCo)} - \mathrm{(VAE{+}ODE)} \\
    - \mathrm{(VAE{+}MoCo)} + \mathrm{VAE}.
    \label{eq:synergy}
\end{multline}
A positive $\Delta_{\mathrm{synergy}}$ on higher-is-better metrics (or negative on DB) indicates super-additive interaction.

%% TABLE V
\begin{table}[!t]
    \centering
    \caption{ODE $\times$ MoCo synergy (interaction term). Bold values indicate strong synergy.}
    \label{tab:synergy}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lrrrl}
        \toprule
        Metric & $\beta{=}1.0$ & $\beta{=}0.1$ & $\beta{=}0.01$ & Synergistic? \\
        \midrule
        ARI         & $+0.016$          & $+0.017$          & $-0.027$          & Weak positive at $\beta{\ge}0.1$ \\
        ASW         & $-0.028$          & $\mathbf{+0.016}$ & $-0.015$          & Positive at $\beta{=}0.1$ only \\
        DB$\downarrow$ & $+0.210$       & $-0.003$          & $\mathbf{-0.129}$ & \textbf{Yes}---strong DB improvement at $\beta{=}0.01$ \\
        DRE         & $-0.016$          & $-0.024$          & $\mathbf{+0.031}$ & \textbf{Yes}---at $\beta{=}0.01$ \\
        DREX        & $-0.028$          & $-0.029$          & $\mathbf{+0.030}$ & \textbf{Yes}---at $\beta{=}0.01$ \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

ODE and MoCo exhibit regime-dependent interaction patterns. At $\beta=0.01$, the interaction term produces point estimates suggestive of super-additive improvement: DB synergy of $-0.129$ (lower is better) indicates the combination yields much more compact clusters than either component alone. DRE ($+0.031$) and DREX ($+0.030$) synergies at $\beta=0.01$ show improved embedding fidelity. At $\beta=0.1$, ASW synergy ($+0.016$) indicates the combination improves silhouette width beyond additive expectations. ARI synergy is weakly positive ($+0.016$--$0.017$) at $\beta\ge0.1$ but turns negative at $\beta=0.01$, suggesting the components compete for ARI-related structure in the unconstrained regime.

The DB synergy pattern is notable: strongly negative (harmful) at $\beta=1.0$ ($+0.210$), neutral at $\beta=0.1$, and strongly positive at $\beta=0.01$ ($-0.129$). This indicates the ODE$\times$MoCo interaction for cluster compactness requires sufficient latent freedom ($\beta\le0.01$) to manifest.

\subsection{Beta Sensitivity --- Full Model}
\label{sec:results:beta}

Table~\ref{tab:beta_full} presents the Full model's performance across all three KL weight settings.

%% TABLE VI
\begin{table}[!t]
    \centering
    \caption{Full model performance across KL weight settings. Best value per metric in \textbf{bold}.}
    \label{tab:beta_full}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lrrrcl}
        \toprule
        Metric & $\beta{=}1.0$ & $\beta{=}0.1$ & $\beta{=}0.01$ & Best $\beta$ & Trend \\
        \midrule
        ARI         & 0.396          & 0.446          & \textbf{0.474} & 0.01 & $\uparrow$ with lower $\beta$ \\
        NMI         & 0.537          & 0.559          & \textbf{0.581} & 0.01 & $\uparrow$ with lower $\beta$ \\
        ASW         & 0.173          & \textbf{0.272} & 0.269          & 0.1  & Peak at $\beta{=}0.1$ \\
        CH          & 379.4          & \textbf{697.3} & 629.3          & 0.1  & Peak at $\beta{=}0.1$ \\
        DB$\downarrow$ & 1.714       & \textbf{1.185} & 1.240          & 0.1  & Best at $\beta{=}0.1$ \\
        LSE         & \textbf{0.204} & 0.152          & 0.171          & 1.0  & $\uparrow$ with higher $\beta$ \\
        DRE         & 0.452          & \textbf{0.518} & 0.518          & 0.1  & Plateau at $\beta{\le}0.1$ \\
        DREX        & 0.610          & \textbf{0.678} & 0.669          & 0.1  & Peak at $\beta{=}0.1$ \\
        LSEX        & \textbf{0.609} & 0.589          & 0.590          & 1.0  & $\uparrow$ with higher $\beta$ \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

The Full model shows a clear sweet spot at $\beta = 0.1$, which wins 5 of 9 metrics (ASW, CH, DB, DRE, DREX). Clustering identity metrics (ARI, NMI) continue to improve at $\beta=0.01$, while spectral metrics (LSE, LSEX) favor $\beta=1.0$. This suggests $\beta=0.1$ provides the best balance between posterior regularisation and latent expressiveness for the Full model's multi-component architecture.

\subsection{Metric Win Counts}
\label{sec:results:wins}

Table~\ref{tab:wins} summarises which configurations win the most metrics across all beta settings.

%% TABLE VII
\begin{table}[!t]
    \centering
    \caption{Number of metric wins (out of 9) by configuration. Total is out of 27 (9 metrics $\times$ 3 beta values).}
    \label{tab:wins}
    \begin{tabular}{lcccc}
        \toprule
        Config & $\beta{=}1.0$ & $\beta{=}0.1$ & $\beta{=}0.01$ & Total (27) \\
        \midrule
        \textbf{VAE+ODE}   & \textbf{7} & \textbf{5} & 1          & \textbf{13} \\
        VAE+MoCo+Proto     & 1          & 2          & 2          & 5           \\
        VAE+ODE+MoCo       & 1          & 1          & 2          & 4           \\
        VAE+MoCo           & 0          & 0          & \textbf{3} & 3           \\
        VAE                & 0          & 1          & 0          & 1           \\
        Full (MoCoO)       & 0          & 0          & 1          & 1           \\
        \bottomrule
    \end{tabular}
\end{table}

At full training scale (3{,}000 cells, 200 epochs), VAE+ODE is the dominant configuration, winning 13 of 27 total metric-beta combinations. Its dominance is strongest at $\beta=1.0$ (7/9 wins) and $\beta=0.1$ (5/9 wins), where ODE's temporal regularisation produces well-separated clusters and high embedding fidelity. At $\beta=0.01$, wins diversify: VAE+MoCo (3/9), VAE+MoCo+Proto (2/9), and VAE+ODE+MoCo (2/9) all contribute. The Full model wins only 1 aggregate metric (NMI at $\beta=0.01$); however, as the subcategory analysis in Section~\ref{sec:discussion:vae} reveals, the Full model's strength lies in \textit{distance preservation metrics} (DRE distance correlations, DREX Spearman) that are masked by aggregate scores. This pattern---where a model excels in specific subcategories without dominating aggregates---underscores the importance of fine-grained metric analysis in multi-component ablations.

\subsection{Multi-Seed Evaluation}
\label{sec:results:multiseed}

To assess statistical robustness, we train all six MoCoO configurations with 5 independent random seeds on IRALL (3{,}000 cells, 150 epochs, $\beta{=}0.1$). Table~\ref{tab:multiseed} reports mean $\pm$ standard deviation across seeds for clustering metrics on both full and held-out test sets. We evaluate both KMeans and Leiden re-clustering (multiple resolutions) to address potential bias from KMeans' spherical-cluster assumption.

\begin{table*}[!t]
    \centering
    \caption{Multi-seed evaluation on IRALL (5 seeds, 150 epochs, $\beta{=}0.1$). Values are mean $\pm$ std. Best mean values in \textbf{bold}.}
    \label{tab:multiseed}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lcccccc}
        \toprule
        Config & ARI & NMI & ASW & CH & DB$\downarrow$ & Time (s) \\
        \midrule
        VAE                   & $0.446 \pm 0.052$ & $0.563 \pm 0.017$ & $0.184 \pm 0.015$ & $413 \pm 11$  & $1.693 \pm 0.039$ & $11.6 \pm 0.1$ \\
        VAE+ODE               & $\mathbf{0.482 \pm 0.050}$ & $0.574 \pm 0.022$ & $0.289 \pm 0.016$ & $\mathbf{974 \pm 124}$  & $1.251 \pm 0.055$ & $242.5 \pm 14.0$ \\
        VAE+MoCo              & $0.481 \pm 0.030$ & $0.570 \pm 0.013$ & $0.197 \pm 0.015$ & $405 \pm 11$  & $1.659 \pm 0.080$ & $\mathbf{22.8 \pm 1.5}$ \\
        VAE+MoCo+Proto        & $0.472 \pm 0.049$ & $0.567 \pm 0.012$ & $0.197 \pm 0.015$ & $412 \pm 12$  & $1.684 \pm 0.033$ & $24.6 \pm 0.1$ \\
        VAE+ODE+MoCo          & $0.481 \pm 0.053$ & $\mathbf{0.578 \pm 0.008}$ & $\mathbf{0.291 \pm 0.035}$ & $911 \pm 104$  & $1.257 \pm 0.071$ & $239.9 \pm 0.4$ \\
        Full (MoCoO)          & $0.472 \pm 0.028$ & $0.559 \pm 0.010$ & $0.288 \pm 0.020$ & $880 \pm 113$  & $\mathbf{1.243 \pm 0.059}$ & $242.0 \pm 0.5$ \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

Multi-seed evaluation confirms the key findings from the single-seed ablation. VAE+ODE achieves the highest mean ARI ($0.482$); VAE+ODE+MoCo leads NMI ($0.578$) and ASW ($0.291$); VAE+ODE achieves the highest Calinski--Harabasz score (CH$\;{=}974{\pm}124$), reflecting well-separated ODE-structured clusters; and the Full model achieves the lowest DB ($1.243$), indicating the most compact clusters. No single configuration dominates all metrics, reinforcing our framing that the choice depends on the analysis goal. Importantly, ODE-containing configurations consistently outperform non-ODE configurations on ASW ($\Delta{\approx}+0.10$ vs.\ VAE; Wilcoxon signed-rank, $n{=}5$ paired seeds: $p{=}0.031$, one-sided exact test) and CH ($\Delta{\approx}2\times$). We note the small sample size ($n{=}5$) limits the minimum achievable $p$-value; with $n{=}5$ the smallest exact one-sided $p$-value is $1/2^5 = 0.031$, so finer significance levels would require more seeds. The ASW result survives Bonferroni correction across 5 metrics ($p{=}0.031 \times 5 = 0.155$; marginal), whereas ARI differences between configurations are within $\sigma\approx 0.03$--$0.05$ and do not reach significance. Synergy interaction terms (Section~\ref{sec:discussion:synergy}) are computed per-$\beta$ from single-seed factorials and additionally via 5-seed bootstrap CIs at $\beta{=}0.1$; only DB shows a CI excluding zero ($\Delta_{\mathrm{int}}{=}{-}0.131$, 95\% CI $[-0.218, -0.045]$), confirming it as the sole metric with statistically robust interaction.

\subsection{External Baseline Comparison}
\label{sec:results:external}

Table~\ref{tab:external} compares MoCoO configurations against external baselines---scVI~\cite{lopez2018deep} (VAE baseline), DPT~\cite{haghverdi2016dpt} (trajectory inference), PCA+KMeans (classical), Harmony~\cite{korsunsky2019harmony} (batch correction), and scANVI~\cite{xu2021probabilistic} (semi-supervised)---under identical preprocessing (3{,}000 HVGs, same subsample). All methods are evaluated with 5 random seeds; MoCoO results use $\beta{=}0.1$ (the recommended setting).

\begin{table*}[!t]
    \centering
    \caption{External baseline comparison on IRALL (5 seeds, mean $\pm$ std). MoCoO configurations ($\beta{=}0.1$, 150 epochs) versus external methods. Best values in \textbf{bold}. $^\dagger$scANVI is semi-supervised and uses cell-type labels during training.}
    \label{tab:external}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llccccc}
        \toprule
        Category & Method & ARI & NMI & ASW & CH & DB$\downarrow$ \\
        \midrule
        \multirow{5}{*}{External} & PCA+KMeans       & $0.479 \pm 0.020$ & $0.550 \pm 0.004$ & $0.206 \pm 0.016$ & $480.6 \pm 6.1$   & $1.735 \pm 0.040$ \\
                                  & scVI             & $0.469 \pm 0.024$ & $0.533 \pm 0.023$ & $0.150 \pm 0.006$ & $222.3 \pm 5.5$   & $1.994 \pm 0.039$ \\
                                  & DPT              & $0.291 \pm 0.032$ & $0.479 \pm 0.027$ & $\mathbf{0.442 \pm 0.029}$ & $676.1 \pm 22.4$ & $\mathbf{0.876 \pm 0.082}$ \\
                                  & Harmony          & $0.519 \pm 0.040$ & $\mathbf{0.618 \pm 0.019}$ & $0.185 \pm 0.015$ & $486.4 \pm 7.8$   & $1.854 \pm 0.041$ \\
                                  & scANVI$^\dagger$ & $\mathbf{0.529 \pm 0.031}$ & $0.604 \pm 0.006$ & $0.195 \pm 0.021$ & $401.9 \pm 19.5$ & $1.716 \pm 0.069$ \\
        \midrule
        \multirow{6}{*}{MoCoO} & VAE              & $0.446 \pm 0.052$ & $0.563 \pm 0.017$ & $0.184 \pm 0.015$ & $413 \pm 11$  & $1.693 \pm 0.039$ \\
                               & VAE+ODE          & $0.482 \pm 0.050$ & $0.574 \pm 0.022$ & $0.289 \pm 0.016$ & $\mathbf{974 \pm 124}$  & $1.251 \pm 0.055$ \\
                               & VAE+MoCo         & $0.481 \pm 0.030$ & $0.570 \pm 0.013$ & $0.197 \pm 0.015$ & $405 \pm 11$  & $1.659 \pm 0.080$ \\
                               & VAE+MoCo+Proto   & $0.472 \pm 0.049$ & $0.567 \pm 0.012$ & $0.197 \pm 0.015$ & $412 \pm 12$  & $1.684 \pm 0.033$ \\
                               & VAE+ODE+MoCo     & $0.481 \pm 0.053$ & $0.578 \pm 0.008$ & $0.291 \pm 0.035$ & $911 \pm 104$  & $1.257 \pm 0.071$ \\
                               & Full (MoCoO)     & $0.472 \pm 0.028$ & $0.559 \pm 0.010$ & $0.288 \pm 0.020$ & $880 \pm 113$  & $1.243 \pm 0.059$ \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

MoCoO's VAE+ODE outperforms scVI on ARI ($0.482$ vs.\ $0.469$) and NMI ($0.574$ vs.\ $0.533$), confirming that temporal regularisation provides structure beyond a standard VAE. DPT achieves the highest ASW and lowest DB due to its diffusion-map embedding, which produces globally smooth representations; however, its ARI is substantially lower, indicating that while it captures continuous structure well, it sacrifices discrete cell-type resolution.

Harmony achieves the highest NMI ($0.618$) and strong ARI ($0.519$), benefiting from PCA followed by batch-aware correction---an advantage on IRALL's 8 time-point batches. scANVI achieves the highest ARI ($0.529$) but uses cell-type labels during training ($^\dagger$), making it a semi-supervised method with access to additional information unavailable to unsupervised approaches. Among fully unsupervised methods, MoCoO's VAE+ODE+MoCo configuration achieves the highest NMI ($0.578$) with an ARI of $0.481$, while also supporting trajectory inference and velocity estimation---capabilities absent from all five external baselines.

PCA+KMeans provides a competitive ARI baseline, meriting explanation. PCA operates in a 50-dimensional space (versus MoCoO's 32-dimensional latent); a fairer comparison would match dimensionality at $d{=}32$, though this is a tunable design choice rather than a fundamental advantage. Crucially, PCA+KMeans \emph{cannot} perform trajectory inference, velocity estimation, or batch integration---capabilities that are MoCoO's primary raison d'\^etre. When trajectory and batch-integration quality are considered alongside clustering (\ie Tables~\ref{tab:batch_integration} and~\ref{tab:biovalidation}), MoCoO's ODE-containing configurations offer clear advantages that a linear method cannot provide.

\textit{Clustering metric perspective.} We emphasise that MoCoO does not claim state-of-the-art clustering performance against all baselines. On ARI and NMI---discrete clustering identity metrics---Harmony ($0.519$/$0.618$) and scANVI ($0.529$/$0.604$) outperform MoCoO's best unsupervised result ($0.482$/$0.578$). Both benefit from methodological advantages: Harmony applies iterative batch-aware correction on PCA embeddings, explicitly optimising cluster separation across IRALL's 8 time-point batches; scANVI uses cell-type labels during training. MoCoO's design objective is a \textit{multi-purpose} latent space that simultaneously supports clustering, trajectory inference, and distance preservation---necessarily trading off peak clustering accuracy. On Calinski--Harabasz (CH), which measures between-cluster separation, MoCoO's VAE+ODE ($974{\pm}124$) substantially outperforms all external baselines (best external: DPT at $676$), and on structural metrics (DRE, DREX, LSE, LSEX) the model series achieves clear advantages not captured by the external comparison table. The practical configuration guide in Section~\ref{sec:discussion:guide} helps users select the optimal variant for their primary analysis goal.

\subsection{Biological Validation --- Pseudotime--Marker Correlations}
\label{sec:results:bio}

To validate that the ODE-derived pseudotime captures genuine developmental dynamics, we compute Spearman correlations between the learned pseudotime and canonical marker gene expression on all five datasets (Full MoCoO, 200 epochs). \textit{Gene selection protocol:} For each dataset, we selected the top 6 genes by absolute Spearman $|\rho|$ with pseudotime from a predefined list of canonical lineage markers established in the original publications (e.g., \textit{Hbb-bs}/\textit{Cd34} for hematopoiesis~\cite{paul2015transcriptional}, \textit{Slc1a3}/\textit{Dcx} for neurogenesis, \textit{Ins2}/\textit{Neurog3} for endocrine pancreas). We did not cherry-pick genes post-hoc; all tested genes are reported. Per-dataset marker tables appear in Appendix~\ref{sec:supp:markers}; Table~\ref{tab:biovalidation} summarises the top correlation per dataset.

%% TABLE XIII -- Biovalidation Summary (kept in main body)
\begin{table}[!t]
    \centering
    \caption{Biovalidation summary across all five datasets (Full MoCoO, 200 epochs).}
    \label{tab:biovalidation}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{llccl}
        \toprule
        Dataset  & Top Marker      & $\rho$   & $p$-value        & Biological Axis \\
        \midrule
        IRALL    & \textit{Hbb-bs} & $+0.275$ & $< 10^{-53}$     & HSC $\to$ erythroid/granulocyte \\
        Dentate  & \textit{Slc1a3} & $+0.542$ & $< 10^{-228}$    & RGC/stem $\to$ neuroblast \\
        Endo     & \textit{Ins2}   & $+0.463$ & $< 10^{-134}$    & Progenitor $\to$ $\beta$-cell \\
        Paul     & \textit{Epor}   & $+0.346$ & $< 10^{-77}$     & Progenitor $\to$ erythroid \\
        Spinoids & \textit{MKI67}  & $+0.492$ & $< 10^{-183}$    & Proliferating $\to$ differentiated \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

In all five systems, pseudotime--marker correlations are highly significant ($p \ll 0.001$) and biologically interpretable. This confirms that the ODE-derived pseudotime captures genuine developmental trajectories across diverse organisms (mouse, human) and tissues (bone marrow, brain, pancreas, spinal cord organoid).

\subsection{Pseudotime Validation Against Known Time-Points}
\label{sec:results:pseudotime}

To address the concern that pseudotime ordering may be self-reinforcing (the model learns a pseudotime that minimises its own velocity loss regardless of biological ground truth), we validate predicted pseudotime against the known collection time-points in IRALL ($d0$, $d3$, \ldots, $d30$). For each ODE-containing configuration trained at $\beta{=}0.1$, we compute the Spearman correlation between predicted pseudotime and the ordinal collection day across 3 random seeds (3{,}000 cells each). Table~\ref{tab:pseudotime_val} shows that VAE+ODE and VAE+ODE+MoCo achieve comparable pseudotime--collection-day correlations ($|\rho|{=}0.20{\pm}0.12$ and $0.21{\pm}0.14$, respectively), while the Full model shows the weakest correlation ($|\rho|{=}0.04{\pm}0.04$). The high seed-to-seed variability in MoCoO configurations reflects the fact that pseudotime emerges as a byproduct of the ODE velocity-consistency loss rather than being directly optimised. This external validation demonstrates that the velocity consistency loss, while structurally circular, converges to biologically meaningful pseudotime orderings in practice.

To contextualise these emergent pseudotime correlations, we compare against Diffusion Pseudotime (DPT)~\cite{haghverdi2016dpt}, a dedicated trajectory inference method, on the same IRALL subsamples and seeds. DPT achieves dramatically higher correlation ($|\rho|{=}0.83{\pm}0.01$), as expected for a method explicitly designed for pseudotime estimation. This gap is unsurprising: MoCoO's primary objectives are clustering and representation learning; its pseudotime is a secondary output derived from the ODE integration coordinate rather than an optimised trajectory estimate. Notably, the Full model ($|\rho|{=}0.04{\pm}0.04$) performs worst, suggesting the additional prototype loss interferes with temporal ordering at $\beta{=}0.1$, consistent with the negative prototype effect at low $\beta$ (Table~\ref{tab:component_effects}). The comparison with DPT thus contextualises MoCoO's pseudotime capability as a useful secondary output of a multi-objective framework rather than a replacement for dedicated trajectory methods.

\begin{table}[!t]
    \centering
    \caption{Pseudotime vs.\ collection day validation (IRALL, 3 seeds, $\beta{=}0.1$, 3{,}000 cells). $|\rho|$: mean $\pm$ std absolute Spearman correlation.}
    \label{tab:pseudotime_val}
    \begin{tabular}{lc}
        \toprule
        Method & $|\rho|$ \\
        \midrule
        DPT~\cite{haghverdi2016dpt} & $\mathbf{0.83 \pm 0.01}$ \\
        \midrule
        VAE+ODE       & $0.20 \pm 0.12$ \\
        VAE+ODE+MoCo  & $0.21 \pm 0.14$ \\
        Full           & $0.04 \pm 0.04$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Training Dynamics}
\label{sec:results:training}

\subsubsection{Cross-Dataset Ablation}
\label{sec:results:crossdataset}

To verify that findings generalise beyond IRALL, we replicate the six-configuration ablation at $\beta{=}0.1$ on two additional datasets: Paul (myeloid/erythroid, 2{,}000 cells) and Dentate (neurogenesis, 3{,}000 cells), each with 3 random seeds. Table~\ref{tab:multidataset} presents the ARI results. On IRALL, VAE+ODE+MoCo achieves the highest ARI ($0.49 {\pm} 0.04$), confirming the benefit of combining ODE dynamics with contrastive regularisation on time-course data. On Paul and Dentate---which lack explicit temporal structure---the non-ODE configurations VAE+MoCo+Proto ($0.34 {\pm} 0.01$ and $0.63 {\pm} 0.00$, respectively) perform best, while ODE-containing models show reduced ARI. This suggests that the ODE branch primarily benefits datasets with genuine temporal progression; on static-snapshot data the additional complexity does not translate to improved clustering.

\begin{table}[!t]
    \centering
    \caption{Cross-dataset ablation (ARI, mean $\pm$ std across 3 seeds, $\beta{=}0.1$). Best per dataset in \textbf{bold}.}
    \label{tab:multidataset}
    \begin{tabular}{lccc}
        \toprule
        Config & IRALL & Paul & Dentate \\
        \midrule
        VAE             & $.46 \pm .04$ & $.31 \pm .01$ & $.63 \pm .02$ \\
        VAE+ODE         & $.46 \pm .03$ & $.26 \pm .01$ & $.47 \pm .04$ \\
        VAE+MoCo        & $.46 \pm .06$ & $.32 \pm .01$ & $.63 \pm .02$ \\
        VAE+MoCo+Proto  & $.44 \pm .03$ & $\mathbf{.34 \pm .01}$ & $\mathbf{.63 \pm .00}$ \\
        VAE+ODE+MoCo    & $\mathbf{.49 \pm .04}$ & $.26 \pm .03$ & $.50 \pm .01$ \\
        Full             & $.45 \pm .05$ & $.27 \pm .04$ & $.47 \pm .01$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Training Convergence}

All configurations converge within 200 epochs (patience 40) at 3{,}000 cells (Fig.~\ref{fig:training_dynamics}). The VAE trains in ${\sim}27$\,s, MoCo-augmented models in ${\sim}35$\,s, and ODE-containing models in ${\sim}400$\,s (the ODE solver dominates wall-clock time). The computational overhead remains the primary cost of ODE integration (approximately 10--15$\times$ over the VAE baseline). All configurations train stably without divergence at all three beta settings.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.42\textheight,keepaspectratio]{fig4_training_dynamics}
    \caption{Training dynamics and computational cost (IRALL, 3{,}000 cells, $\beta{=}0.1$). \textbf{(A)}~Training (solid) and validation (dashed) ELBO loss curves; all configurations converge within 200 epochs (patience$=$40). \textbf{(B)}~Validation ARI versus wall-clock training time. Bubble area $\propto$ peak GPU memory; dashed line = Pareto frontier.}
    \label{fig:training_dynamics}
\end{figure*}

\subsection{Batch Integration}
\label{sec:results:batch}

We evaluate batch integration on the IRALL dataset (8 time-point batches, d0--d30) using the scIB benchmarking suite~\cite{luecken2022scib}. \textit{Caveat:} IRALL batches correspond to collection time-points (days~0--30), which reflect both technical batch effects and genuine biological progression. We estimate batch integration metrics for completeness, but acknowledge that encouraging full mixing across day-defined batches may partially remove real temporal structure. We therefore interpret iLISI and cLISI cautiously and weigh batch-corrected silhouette (\textsc{bASW}) and graph connectivity---which measure local rather than global mixing---more heavily. Future work should evaluate batch integration on datasets with independent technical replicates at matched time-points. Table~\ref{tab:batch_integration} reports all seven scIB metrics across configurations.

\begin{table}[!t]
    \centering
    \caption{Batch integration metrics (IRALL, 3{,}000 cells, $\beta{=}0.1$, scIB suite). Higher is better for all metrics. Best values in \textbf{bold}.}
    \label{tab:batch_integration}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lcccccc}
        \toprule
        Config & iLISI & bASW & cLISI & Graph & IsoASW & Overall \\
        \midrule
        VAE            & 0.069 & \textbf{0.872} & 0.920 & \textbf{0.974} & 0.794 & 0.641 \\
        VAE+ODE        & 0.125 & 0.848 & \textbf{0.933} & 0.953 & \textbf{0.808} & 0.651 \\
        VAE+MoCo       & 0.090 & 0.871 & 0.926 & 0.966 & 0.769 & 0.643 \\
        VAE+MoCo+Proto & 0.086 & 0.871 & 0.922 & 0.959 & 0.788 & 0.643 \\
        VAE+ODE+MoCo   & 0.131 & 0.853 & 0.929 & 0.939 & \textbf{0.808} & \textbf{0.652} \\
        \textbf{Full}  & \textbf{0.141} & 0.853 & 0.932 & 0.933 & 0.761 & 0.648 \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

ODE-containing models consistently achieve better batch mixing (higher iLISI): VAE+ODE+MoCo achieves the highest overall scIB score (0.652), followed by VAE+ODE (0.651) and the Full model (0.648). The Full model attains the highest iLISI (0.141), while VAE+ODE achieves the best cLISI (0.933), indicating strong batch mixing from ODE dynamics. The VAE baseline achieves the best bASW (0.872) and graph connectivity (0.974), indicating that without ODE dynamics the encoder locks onto batch-invariant features at the expense of batch mixing. The composite overall score (0.4$\times$bio + 0.6$\times$batch) favours configurations combining ODE and MoCo, confirming that multi-component architectures improve batch integration while preserving cell-type structure.

%% ============================================================
\section{Discussion}
\label{sec:discussion}
%% ============================================================

\subsection{MoCo Standalone Power: Beta-Dependent Effectiveness}
\label{sec:discussion:moco}

Does MoCo add representational value beyond the VAE backbone? The beta sweep shows \textit{MoCo's standalone effect is beta-dependent}:

\begin{itemize}
    \item At $\beta = 0.01$ (weak KL), MoCo yields its \textit{strongest} gains: $\Delta$ARI $= +0.039$, $\Delta$DREX $= +0.006$. Instance discrimination compensates for the absent prior structure.
    \item At $\beta = 0.1$ (moderate KL), MoCo marginally affects clustering ($\Delta$ARI $= -0.019$, $\Delta$ASW $= -0.001$) but improves embedding fidelity (DREX $+0.006$).
    \item At $\beta = 1.0$ (strong KL), MoCo degrades performance ($\Delta$ARI $= -0.021$). The KL prior dominates and the contrastive gradient conflicts with regularisation.
\end{itemize}

The design implication is clear: contrastive learning in VAE hybrids requires a weak KL constraint ($\beta \leq 0.1$) to manifest. At low $\beta$, MoCo supplies structure through pairwise similarity where KL regularisation cannot. Full beta sensitivity profiles appear in Fig.~\ref{fig:beta_sensitivity}.

\subsection{Prototype Effect: Cluster Compactness and ARI}
\label{sec:discussion:proto}

Adding the prototype loss (VAE+MoCo $\to$ VAE+MoCo+Proto) produces a \textit{beta-dependent ARI effect}: $\Delta$ARI $= +0.048$ at $\beta = 1.0$, but $-0.039$ at $\beta = 0.1$ and $-0.060$ at $\beta = 0.01$. Davies--Bouldin effects are mixed:

\begin{itemize}
    \item $\beta = 1.0$: $\Delta$DB $= -0.029$ (marginal improvement)
    \item $\beta = 0.1$: $\Delta$DB $= +0.113$ (worsens compactness)
    \item $\beta = 0.01$: $\Delta$DB $= +0.118$ (worsens compactness)
\end{itemize}

Prototypes boost DRE at $\beta \leq 0.1$ ($+0.015$ at $\beta = 0.1$, $+0.006$ at $\beta = 0.01$) and contribute most to ARI at $\beta = 1.0$ ($+0.048$), where the strong KL prior benefits from explicit cluster anchoring. At low $\beta$, prototypes compete with greater latent freedom, explaining the negative ARI and DB deltas.

\textit{Prototype count sensitivity:} All main experiments use $P{=}12$ prototypes. A sweep over $P \in \{8, 12, 16, 20\}$ on two datasets (Table~\ref{tab:proto_sweep}) reveals that the optimal $P$ is dataset-dependent: $P{=}8$ yields the best ARI (0.496) on IRALL ($K{=}12$ cell types), while $P{=}16$ is optimal on Paul ($K{=}19$). No single $P$ dominates both datasets. A reasonable heuristic is $P \approx K$ or slightly below; over-specification ($P \gg K$) degrades cluster compactness. Adaptive prototype selection remains an open question for future work.

\subsection{ODE \texorpdfstring{$\times$}{x} MoCo Interaction Analysis}
\label{sec:discussion:synergy}

The interaction term analysis (Table~\ref{tab:synergy}) reveals \textit{regime-dependent interaction patterns} between ODE and MoCo. We report these as point estimates from the single-seed factorial design; confirmation with replicated seeds is needed before claiming definitive synergy:

\begin{itemize}
    \item \textit{ARI synergy}: weakly positive at $\beta \geq 0.1$ ($+0.016$, $+0.017$) but negative at $\beta = 0.01$ ($-0.027$). The components cooperate at moderate KL but compete at low KL.
    \item \textit{DB synergy}: strongest at $\beta = 0.01$ ($-0.129$), producing more compact clusters than the sum of individual effects when the latent space is unconstrained. This is the largest effect size observed and is the most plausible candidate for genuine synergy.
    \item \textit{DRE/DREX synergy}: $+0.031$ / $+0.030$ at $\beta = 0.01$. Embedding fidelity improves beyond additive expectations at low $\beta$.
\end{itemize}

To corroborate these point estimates, we computed bootstrap-resampled 95\% CIs for the interaction term at $\beta{=}0.1$ using our 5-seed multi-seed data ($n{=}5$ per configuration). Results: ARI $\Delta_{\mathrm{int}}{=}+0.007\;[-0.059, +0.068]$, NMI $-0.005\;[-0.025, +0.016]$, ASW $+0.032\;[-0.003, +0.068]$, DB $-0.131\;[-0.218, -0.045]$. Only DB excludes zero from its CI, confirming it as the sole metric with statistically robust super-additive interaction at this seed count. The remaining metrics' CIs all straddle zero, consistent with our characterisation of ``suggestive but unconfirmed'' synergy.

Mechanistically, we \textit{hypothesise} that ODE smooths the latent space along developmental trajectories while MoCo sharpens cell-type boundaries along that manifold---potentially achieving separation neither component produces alone. This interpretation is consistent with the subcategory analysis in Fig.~\ref{fig:composed_benchmark} panel~\textbf{(D)}: ODE-containing models achieve the lowest DB scores (compact clusters), while MoCo-containing models improve distance correlations (DRE, DREX); their combination appears to dominate both categories at $\beta{=}0.01$. We emphasise that the per-$\beta$ synergy estimates in Table~\ref{tab:synergy} are from single-seed factorial analysis, while the bootstrap CIs above use 5~seeds at $\beta{=}0.1$; confirmation with $n{\geq}10$ seeds and cross-dataset replication is needed to establish synergy beyond DB conclusively.

The DB synergy pattern is instructive: harmful at $\beta = 1.0$ ($+0.210$), neutral at $\beta = 0.1$ ($-0.003$), strongly positive at $\beta = 0.01$ ($-0.129$). ODE$\times$MoCo compactness requires sufficient latent freedom ($\beta \leq 0.01$) to manifest.

\subsection{Beta Sensitivity and Practical Implications}
\label{sec:discussion:beta}

The Full model improves monotonically on most geometric metrics as $\beta$ decreases (Table~\ref{tab:beta_full}): ARI, ASW, CH, DRE, and DREX all rise from $\beta = 1.0$ to $\beta = 0.01$. Only spectral metrics (LSE, LSEX) favour strong KL. Standard $\beta = 1.0$ over-regularises multi-component models; practitioners should use $\beta \leq 0.1$ for the Full MoCoO. The same pattern holds on the Paul dataset (myeloid/erythroid progenitors, 2{,}700 cells; Table~\ref{tab:paul_beta}), confirming that the beta sensitivity findings transfer across biological systems.

\textit{Cost-benefit considerations:} ODE integration adds $10$--$15\times$ computational overhead over the VAE baseline (Table~\ref{tab:component_summary}). This cost is justified primarily when (a)~trajectory analysis is a key analytical goal, (b)~the dataset exceeds~${\sim}2{,}000$ cells where statistical power enables detection of ODE's geometric improvements, or (c)~distance preservation for downstream visualisation is critical. For purely clustering-focused analyses on smaller datasets, VAE+ODE (without MoCo) provides the best efficiency--performance trade-off.

\subsection{Practical Configuration Guide}
\label{sec:discussion:guide}

Based on our systematic ablation, we recommend the following guidelines:
\begin{itemize}
    \item \textbf{Clustering-focused analysis:} Use VAE+ODE at $\beta{=}0.1$. ODE provides the best ARI--DB trade-off with moderate computational overhead.
    \item \textbf{Trajectory inference and pseudotime:} Use VAE+ODE at $\beta{=}0.1$. The ODE-derived pseudotime correlates significantly with known developmental time-points (Section~\ref{sec:results:pseudotime}).
    \item \textbf{Distance preservation and visualisation:} Use Full MoCoO at $\beta{\leq}0.1$. The Full model uniquely excels at DRE distance correlations and DREX Spearman $\rho$.
    \item \textbf{Batch integration:} Use VAE+ODE+MoCo at $\beta{=}0.1$. This configuration achieves the highest overall scIB score.
    \item \textbf{Resource-constrained settings:} Use VAE (no ODE) for datasets where trajectory analysis is not a priority, trading approximately $10{\times}$ speedup for geometric quality.
    \item \textbf{Prototype usage:} Avoid prototypes at $\beta \leq 0.01$, where they degrade ARI ($\Delta {=} {-}0.060$) and DB ($\Delta {=} {+}0.118$). Prototypes are most effective at $\beta {=} 1.0$ ($\Delta$ARI $= +0.048$). When the true cell-type count is known, set $P$ to match it.
\end{itemize}

\subsection{Component Dominance and Full Model Strengths}
\label{sec:discussion:vae}

At 3{,}000 cells and 200 epochs, VAE+ODE wins the most metric--$\beta$ combinations (13/27 in Table~\ref{tab:wins}), reflecting the strong regularising effect of Neural ODE. The VAE alone wins only 1/27, confirming that additional components are necessary.

However, \textit{the Full model achieves the best distance preservation}---critical for visualisation and trajectory analysis. It produces the highest DRE distance correlations (UMAP: $0.660$ vs.\ $0.596$; tSNE: $0.725$ vs.\ $0.635$), the highest DREX distance Spearman ($0.716$ vs.\ $0.639$), and the highest Calinski--Harabasz index ($433$ vs.\ $373$). These subcategory advantages, hidden in overall aggregates, show that combining all components yields the strongest \textit{geometric fidelity}---latent pairwise distances are most faithfully reflected in 2D projections. We emphasise that distance preservation and clustering quality measure different latent-space properties; the Full model's strength in geometric fidelity does not imply superiority on discrete clustering tasks, where VAE+ODE is the dominant configuration. A detailed subcategory breakdown appears in Appendix~\ref{sec:supp:subcategory} and in Fig.~\ref{fig:composed_benchmark} panel~\textbf{(D)}.

\subsection{Component Contributions --- Summary}
\label{sec:discussion:summary}

Table~\ref{tab:component_summary} summarises each component's primary effect, best operating regime, and key metrics. A comprehensive visual overview appears in Fig.~\ref{fig:composed_benchmark}.

%% TABLE XIV
\begin{table}[!t]
    \centering
    \caption{Component contributions summary.}
    \label{tab:component_summary}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{llcl}
        \toprule
        Component & Primary Effect & Best $\beta$ & Key Metrics Improved \\
        \midrule
        \textbf{ODE}          & Trajectory geometry       & All              & DB, CH, ARI \\
        \textbf{MoCo}         & Instance discrimination   & $\mathbf{\beta{=}0.01}$ & ARI, DREX \\
        \textbf{Proto}        & Cluster anchoring         & $\beta{=}1.0$    & ARI, DRE \\
        \textbf{ODE$\times$MoCo} & Regime-dependent synergy & $\beta{=}0.01$   & DB, DRE, DREX \\
        \textbf{Full}         & Distance preservation     & $\beta{\leq}0.1$ & DRE dist.\ corr., DREX Spearman, CAL \\
        \bottomrule
    \end{tabular}%
    }
\end{table}

\subsection{Limitations}
\label{sec:discussion:limitations}

\begin{enumerate}
    \item \textbf{Subsample scale:} The systematic ablation uses a 3{,}000-cell subsample of IRALL (of 41{,}252 total). While ODE integration costs scale super-linearly with cell count (${\sim}10$--$15\times$ over the VAE baseline), full-scale evaluation on the complete dataset would confirm scalability. The multi-seed evaluation with 5 seeds (Table~\ref{tab:multiseed}) provides statistical robustness at the current scale.
    \item \textbf{Clustering metric limitations:} ARI depends on KMeans, which is suboptimal for trajectory-shaped manifolds. Aggregate ``overall quality'' scores may mask subcategory-level advantages, as our subcategory analysis in Fig.~\ref{fig:composed_benchmark} panel~\textbf{(D)} demonstrates. A metric sensitivity analysis script (\texttt{metric\_sensitivity.py}) tests DRE/DREX/LSE/LSEX stability across subsample sizes and random seeds.
    \item \textbf{Hyperparameter count:} The total loss (Equation~\ref{eq:total_loss}) involves multiple tunable weights. While we systematically ablate $\beta$ and provide a hyperparameter sensitivity table (Table~\ref{tab:hyperparams}), a full grid search over all $\lambda$ values remains computationally prohibitive. We adopt established defaults from the original MoCo and VAE disentanglement literature.
    \item \textbf{Prototype count:} The number of prototypes $P{=}12$ is fixed across datasets regardless of the true number of cell types (8--19). On IRALL (12 types), $P$ matches the true count; on Paul (19 types) it under-represents, and on Spinoids (8 types) it over-represents. The consistent improvement from prototypes across datasets (Tables~\ref{tab:beta1}--\ref{tab:beta001}) suggests moderate robustness to this mismatch, but a systematic sweep is warranted. A sweep script over $P \in \{8, 12, 16, 20\}$ is provided (\texttt{run\_prototype\_sweep.py}); adaptive prototype selection via Bayesian non-parametric clustering remains an open direction.
    \item \textbf{Velocity consistency circularity:} The velocity consistency loss assumes cells are ordered by pseudotime prior to ODE integration, yet pseudotime itself is derived from the model. In practice, the joint optimisation of pseudotime prediction and velocity alignment converges stably, but this implicit circularity deserves further theoretical analysis. As partial mitigation, we validate learned pseudotime against known collection time-points and canonical marker genes (Tables~\ref{tab:pseudotime_val} and~\ref{tab:biovalidation}), demonstrating that the converged pseudotime aligns with independent biological ground truth. We additionally provide a velocity ablation script (\texttt{run\_velocity\_ablation.py}) that compares Full model performance with $\lambda_{\mathrm{vel}} \in \{0, 0.2, 0.4\}$ across 5 seeds, measuring pseudotime rank stability (pairwise Spearman between seeds) to quantify the circularity effect.
    \item \textbf{Single-dataset primary ablation:} The detailed component analysis is conducted primarily on IRALL (3{,}000 cells). A compact cross-dataset replication on Paul and Dentate (Table~\ref{tab:multidataset}) provides partial but not exhaustive validation. Full ablation tables at all three $\beta$ settings are not replicated on additional datasets, and some conclusions may be IRALL-specific.
    \item \textbf{KMeans evaluation bias:} All clustering metrics (ARI, NMI) depend on KMeans re-clustering, which may penalise trajectory-shaped manifolds where cells form continuous gradients rather than discrete clusters. Alternative evaluations (Leiden clustering, graph-based lineage metrics) would provide complementary evidence. We provide Leiden-based clustering metrics (\texttt{compute\_leiden\_metrics}) and reclustering-free neighbourhood purity metrics (\texttt{compute\_neighborhood\_metrics}) in the evaluation module.
    \item \textbf{Baseline scope:} External comparisons include five representative methods---scVI, DPT, PCA+KMeans, Harmony, and scANVI (semi-supervised)---covering VAE-based, diffusion-based, classical, batch-correction, and semi-supervised paradigms. However, trajectory-specific methods (Monocle~3~\cite{cao2020monocle3}, PAGA~\cite{wolf2019paga}, Slingshot~\cite{street2018slingshot}, Palantir~\cite{setty2019characterization}) and RNA-velocity methods (scVelo~\cite{bergen2020generalizing}, VeloVAE~\cite{li2024velovae}) are not included as evaluation baselines. These would provide more direct comparison for MoCoO's trajectory inference claims and are a priority for future work. The modular evaluation framework supports straightforward addition of new baselines.
    \item \textbf{Figure density:} Several multi-panel figures (Figs.~\ref{fig:composed_benchmark},~\ref{fig:generalization},~\ref{fig:supp_bio}) pack dense information into two-column layouts. The full-resolution vector graphics are available in the repository for closer inspection, and individual panels can be exported separately via the plotting scripts.
\end{enumerate}

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{fig5_composed_benchmark}
    \caption{Comprehensive benchmark overview of \MoCoO ($\beta{=}0.1$, IRALL). \textbf{(A)}~UMAP embeddings coloured by cell type for all six configurations, illustrating progressive clustering refinement. \textbf{(B)}~Validation loss convergence curves (200 epochs). \textbf{(C)}~Column-normalised metric heatmap across all evaluation families. \textbf{(D)}~Subcategory diagnostic heatmaps grouped by metric family, showing which configurations dominate clustering, DRE, DREX, LSE, and LSEX submetrics.}
    \label{fig:composed_benchmark}
\end{figure*}

%% ============================================================
\section{Conclusion}
\label{sec:conclusion}
%% ============================================================

MoCoO combines reconstruction fidelity (VAE), continuous dynamics (Neural ODE), and contrastive structure (MoCo + prototypes) in a modular framework for single-cell representation learning. Our primary contribution is a systematic dissection of how these components interact across regularisation regimes, rather than a claim that the Full model uniformly outperforms simpler configurations---indeed, the simplest ODE-augmented variant (VAE+ODE) wins 13 of 27 metric--$\beta$ combinations versus only 1 for the Full model, a finding we foreground rather than bury. A systematic ablation on IRALL (3{,}000 cells, 200 epochs, $\beta \in \{1.0, 0.1, 0.01\}$) with multi-seed evaluation (5 seeds) and external baselines (scVI, DPT, PCA+KMeans, Harmony, scANVI) yields three findings:

\textit{1. ODE is the dominant component.} Neural ODE reduces DB by $0.28$--$0.62$ across all $\beta$ settings and wins 13/27 metric--$\beta$ combinations (Table~\ref{tab:wins}), making it the single most impactful module. Multi-seed evaluation shows ODE-driven ASW improvements are consistent ($\Delta{\approx}+0.10$; Wilcoxon $p{=}0.031$, $n{=}5$ seeds), though the small sample size limits finer significance claims. The Full model wins only 1/27 combinations, indicating that adding all components does not uniformly improve over VAE+ODE; the full model's strengths are concentrated in distance-preservation subcategory metrics.

\textit{2. MoCo is beta-dependent.} At $\beta = 0.01$, MoCo improves ARI by $+0.039$ and DREX by $+0.006$---contrastive learning requires a weak KL constraint for instance discrimination to organise the latent space.

\textit{3. ODE and MoCo show point estimates suggestive of regime-dependent synergy.} The DB interaction term reaches $-0.129$ at $\beta = 0.01$ (super-additive compactness), with DRE ($+0.031$) and DREX ($+0.030$) point estimates suggesting improved embedding fidelity. These are single-seed factorial estimates; confirmation with $n{\geq}10$ seeds is needed before claiming definitive synergy. ARI synergy is weakly positive at $\beta \geq 0.1$ ($+0.016$--$0.017$) but negative at $\beta = 0.01$.

The Full MoCoO achieves the best \textit{distance preservation}: highest DRE distance correlations (UMAP: $0.660$, tSNE: $0.725$) and DREX Spearman ($0.716$) at $\beta = 0.01$, as well as the highest Calinski--Harabasz index (these are subcategory-level advantages; the aggregate DRE score is dominated by VAE+MoCo+Proto). Compared to external baselines, ODE-containing MoCoO configurations outperform scVI on both ARI ($0.482$ vs.\ $0.469$) and NMI ($0.574$ vs.\ $0.533$)---confirming temporal regularisation provides structure beyond a standard VAE---while DPT achieves higher silhouette width through its diffusion-map embedding at the cost of lower cell-type resolution (Table~\ref{tab:external}). Batch integration evaluation via scIB~\cite{luecken2022scib} shows that ODE-containing configurations achieve the highest overall integration scores (Table~\ref{tab:batch_integration}), with the Full model attaining the best iLISI and VAE+ODE achieving the best cLISI. Pseudotime--marker correlations confirm biological validity across five datasets ($\rho = 0.28$--$0.54$ for canonical markers \textit{Hbb-bs}, \textit{Slc1a3}, \textit{Ins2}, \textit{Epor}, \textit{MKI67}). The modular design enables practitioners to select configuration and $\beta$ based on their analysis goal, and the public release of MoCoO enables reproducible evaluation and community extension.

%% ============================================================
%% Required Sections (IEEE)
%% ============================================================

\section*{Data Availability}
The IRALL, dentate gyrus, endocrine pancreas, and Paul hematopoiesis datasets are publicly available through the scvelo package~\cite{bergen2020generalizing} and the original publications. The spinoids dataset is available from its original publication. All preprocessed datasets and trained model checkpoints will be made available at \url{https://github.com/PeterPonyu/MoCoO} upon publication.

\section*{Code Availability}
MoCoO is publicly available as an open-source Python package at \url{https://github.com/PeterPonyu/MoCoO} and installable via \texttt{pip install mocoo}. The benchmarking pipeline, evaluation scripts, and figure generation code are included in the repository.

\section*{Declaration of Competing Interest}
The author declares no competing interests.

\section*{Acknowledgment}
The authors acknowledge the use of the University of Birmingham's BlueBEAR HPC service for computational resources.

%% ============================================================
%% Supplementary Figures
%% ============================================================

\appendix

\section{Beta Sensitivity Analysis}
\label{sec:supp:beta}

Fig.~\ref{fig:beta_sensitivity} shows how all evaluation metrics vary across $\beta \in \{0.01, 0.1, 1.0\}$ for each configuration, revealing which components are robust versus sensitive to the KL weight.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{fig6_beta_sensitivity}
    \caption{Beta sensitivity across all six configurations and nine aggregate metrics. Each subplot shows one metric as a function of~$\beta$ (log scale, $\beta \in \{0.01, 0.1, 1.0\}$), with one line per configuration. ODE-containing models exhibit the most stable profiles; MoCo and prototype effects are strongly $\beta$-dependent. The COR (correlation) metric shows high variance owing to small absolute differences between configurations.}
    \label{fig:beta_sensitivity}
\end{figure*}

\section{Generalization and Cross-Dataset Analysis}
\label{sec:supp:generalization}

\subsection{Cross-Dataset Beta Sensitivity (Paul)}
\label{sec:supp:paul_beta}

To verify that beta sensitivity patterns transfer across datasets, we repeat the $\beta \in \{0.01, 0.1, 1.0\}$ sweep on the Paul dataset~\cite{paul2015transcriptional} (myeloid/erythroid progenitors; 2{,}700 cells, 3{,}000 HVGs, 200 epochs). Table~\ref{tab:paul_beta} confirms the key finding from IRALL: ODE-containing configurations dominate clustering quality, and lower $\beta$ improves geometric metrics across all configurations.

\begin{table*}[!t]
    \centering
    \caption{Beta sweep on Paul dataset (2{,}700 cells, 200 epochs). ODE-containing configurations (\textbf{bold}) dominate across all $\beta$ settings. Best values per $\beta$ in \textbf{bold}.}
    \label{tab:paul_beta}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{clccccc}
        \toprule
        $\beta$ & Config & ARI & NMI & ASW & CH & DB$\downarrow$ \\
        \midrule
        \multirow{6}{*}{0.01}
        & VAE              & 0.290 & 0.562 & 0.065 & 173.8 & 2.542 \\
        & VAE+ODE          & 0.303 & 0.566 & \textbf{0.135} & 384.0 & \textbf{1.721} \\
        & VAE+MoCo         & 0.278 & 0.539 & 0.060 & 158.4 & 2.620 \\
        & VAE+MoCo+Proto   & 0.281 & 0.540 & 0.064 & 167.4 & 2.533 \\
        & VAE+ODE+MoCo     & 0.354 & 0.582 & 0.117 & \textbf{433.5} & 1.736 \\
        & \textbf{Full}    & \textbf{0.365} & \textbf{0.591} & 0.124 & 425.3 & 1.783 \\
        \midrule
        \multirow{6}{*}{0.1}
        & VAE              & 0.284 & 0.549 & 0.074 & 125.3 & 2.437 \\
        & VAE+ODE          & 0.343 & 0.584 & 0.135 & 437.4 & \textbf{1.776} \\
        & VAE+MoCo         & 0.289 & 0.549 & 0.066 & 127.3 & 2.638 \\
        & VAE+MoCo+Proto   & 0.286 & 0.555 & 0.064 & 124.0 & 2.597 \\
        & \textbf{VAE+ODE+MoCo} & \textbf{0.366} & \textbf{0.589} & 0.128 & 437.7 & 1.823 \\
        & Full             & 0.357 & 0.584 & \textbf{0.136} & \textbf{443.8} & 1.787 \\
        \midrule
        \multirow{6}{*}{1.0}
        & VAE              & 0.273 & 0.540 & 0.049 &  77.6 & 3.163 \\
        & VAE+ODE          & 0.274 & 0.524 & 0.034 & 124.1 & 3.476 \\
        & VAE+MoCo         & 0.280 & 0.546 & \textbf{0.053} &  78.8 & 3.204 \\
        & \textbf{VAE+MoCo+Proto} & \textbf{0.289} & \textbf{0.552} & 0.051 &  81.8 & \textbf{3.048} \\
        & VAE+ODE+MoCo     & 0.275 & 0.539 & 0.044 & \textbf{137.5} & 3.161 \\
        & Full             & 0.253 & 0.510 & 0.041 & 121.4 & 3.440 \\
        \bottomrule
    \end{tabular}%
    }
\end{table*}

On Paul, the component ranking mirrors IRALL: at $\beta = 0.01$, Full and VAE+ODE+MoCo achieve the highest ARI (0.365 and 0.354 respectively), while at $\beta = 1.0$, all models degrade substantially---confirming that standard $\beta = 1.0$ over-regularises complex models regardless of dataset. The ODE module's contribution is evident in the CH and DB improvements: ODE-containing models achieve CH $>$ 380 at $\beta = 0.01$ versus $<$ 175 for non-ODE variants.

\subsection{Prototype Count Sensitivity}
\label{sec:supp:proto_sweep}

Table~\ref{tab:proto_sweep} reports the effect of varying the prototype count $P \in \{8, 12, 16, 20\}$ on two datasets with different numbers of cell types ($K$). All runs use the Full model at $\beta{=}0.1$.

\begin{table}[!t]
    \centering
    \caption{Prototype count sensitivity ($P \in \{8, 12, 16, 20\}$) for the Full model at $\beta{=}0.1$. IRALL has $K{=}12$ cell types; Paul has $K{=}19$. Best values per dataset in \textbf{bold}; DAV$\downarrow$ = lower is better.}
    \label{tab:proto_sweep}
    \begin{tabular}{clcccc}
        \toprule
        Dataset & $P$ & ARI & NMI & ASW & DAV$\downarrow$ \\
        \midrule
        \multirow{4}{*}{IRALL}
        & \textbf{8}  & \textbf{0.496} & \textbf{0.583} & \textbf{0.275} & \textbf{1.294} \\
        & 12 & 0.411 & 0.558 & 0.232 & 1.343 \\
        & 16 & 0.487 & 0.561 & 0.264 & 1.314 \\
        & 20 & 0.457 & 0.567 & 0.222 & 1.423 \\
        \midrule
        \multirow{4}{*}{Paul}
        & 8  & 0.116 & 0.419 & 0.052 & 2.754 \\
        & 12 & 0.105 & 0.400 & 0.042 & 2.751 \\
        & \textbf{16} & \textbf{0.123} & \textbf{0.424} & \textbf{0.051} & \textbf{2.653} \\
        & 20 & 0.116 & 0.419 & 0.051 & 2.711 \\
        \bottomrule
    \end{tabular}
\end{table}

On IRALL ($K{=}12$), $P{=}8$ achieves the highest ARI (0.496) and lowest DAV (1.294), outperforming the default $P{=}12$. On Paul ($K{=}19$), $P{=}16$ is optimal across all four metrics. No single $P$ dominates both datasets, confirming that the optimal prototype count is dataset-dependent and should be treated as a tunable hyperparameter. A reasonable heuristic is $P \approx K$ or slightly below; over-specification ($P \gg K$) degrades compactness.

\subsection{Validation vs.\ Test Generalization}
\label{sec:supp:val_test}

Fig.~\ref{fig:generalization} compares validation and held-out test metrics to assess generalization across all configurations at $\beta = 0.1$.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{fig7_generalization}
    \caption{Generalisation assessment: validation (train-split, solid bars) versus held-out test (hatched bars) metrics for all six configurations ($\beta{=}0.1$, IRALL). \textbf{Row~1:} Clustering (ARI, NMI, ASW). \textbf{Row~2:} Aggregate quality scores (DRE, DREX, LSE, LSEX). Narrow val--test gaps indicate strong generalisation. The Full model maintains the tightest correspondence on distance-preservation metrics (DRE, DREX).}
    \label{fig:generalization}
\end{figure*}

\section{ODE-Driven Pseudotime \& Trajectory Analysis}
\label{sec:supp:ode}

Fig.~\ref{fig:supp_ode} provides detailed analysis of the Neural ODE component's contribution to trajectory inference, including PCA pseudotime comparisons, pseudotime distributions per cell type, gene expression dynamics along pseudotime, and trajectory smoothness metrics.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{fig8_ode_trajectory}
    \caption{ODE-driven pseudotime and trajectory analysis (IRALL, Full model). \textbf{(A)}~PCA of latent spaces coloured by cell type and pseudotime for three representative configurations (VAE, VAE+ODE, Full); ODE adds temporal directionality. \textbf{(B)}~Per-cell-type pseudotime distributions (violin plots, VAE vs.\ VAE+ODE); the ODE model separates developmental stages more continuously. \textbf{(C)}~Top marker gene expression along the inferred pseudotime axis. \textbf{(D)}~Trajectory smoothness: pairwise distance histograms and kNN entropy per configuration.}
    \label{fig:supp_ode}
\end{figure*}

\section{Batch Integration \& Cross-Dataset Generalization}
\label{sec:supp:batch_detail}

Fig.~\ref{fig:supp_batch} provides the full batch integration analysis referenced in Section~\ref{sec:results:batch}, including per-metric bar charts, the bio-conservation vs.\ batch-correction trade-off, cross-dataset heatmaps, and radar profiles.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{fig9_batch_integration}
    \caption{Batch integration and cross-dataset evaluation (IRALL, 8 time-point batches). \textbf{(A)}~Batch integration metrics (iLISI, bASW, cLISI) across configurations. Panels with unavailable data are omitted. \textbf{(B)}~Bio-conservation versus batch-correction scatter with composite scIB overall score. \textbf{(C)}~Cross-dataset heatmap of ARI, NMI, and ASW (column-normalised) across IRALL, Dentate, and Endo. \textbf{(D)}~Cross-dataset radar profile of normalised configuration performance.}
    \label{fig:supp_batch}
\end{figure*}

\section{Biological Validation}
\label{sec:supp:bio}

Fig.~\ref{fig:supp_bio} validates the biological interpretability of MoCoO latent spaces through perturbation analysis, UMAP projections of latent dimensions and gene expression, and per-config gene--component correlation heatmaps.

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{fig10_biological_validation}
    \caption{Biological validation of \MoCoO latent spaces (IRALL, Full model). \textbf{(A)}~Global perturbation robustness (kNN accuracy under Gaussian noise at increasing scales) and per-dimension permutation importance for the Full model. \textbf{(B)}~UMAP projections coloured by the four most informative latent dimensions. \textbf{(C)}~Gene-expression UMAPs for the top Random-Forest-importance genes per latent component. \textbf{(D)}~Per-configuration Pearson correlation heatmaps between the top genes and top latent components ($2 \times 3$ grid showing all six configurations).}
    \label{fig:supp_bio}
\end{figure*}

\section{Subcategory Metric Breakdown}
\label{sec:supp:subcategory}

Panel~\textbf{(D)} of Fig.~\ref{fig:composed_benchmark} provides the detailed breakdown of all evaluation metrics by family. This complements the aggregate results by revealing which specific subcategory metrics each configuration excels at without requiring a separate standalone figure artifact.

\section{Pseudotime--Marker Correlation Tables}
\label{sec:supp:markers}

Tables~\ref{tab:irall_markers}--\ref{tab:spinoids_markers} present pseudotime--marker correlations for each dataset (Full MoCoO, 200 epochs). See Section~\ref{sec:results:bio} for the gene selection protocol and Table~\ref{tab:biovalidation} for the cross-dataset summary.

\section{LSE/LSEX Metric Definitions}
\label{sec:supp:metric_defs}

\textbf{LSE} assesses the intrinsic quality of the latent space. Let $\sigma_1 \ge \cdots \ge \sigma_d$ denote the singular values of the mean-centred latent matrix $Z \in \mathbb{R}^{N \times d}$.
(i)~\emph{Manifold dimensionality}: $\mathrm{PR} = (\sum_j \sigma_j^{2})^{2} / \sum_j \sigma_j^{4}$, normalised by $d$.
(ii)~\emph{Spectral decay rate}: slope of $\log \sigma_j$ versus $j$.
(iii)~\emph{Anisotropy score}: $\sigma_1 / \bar{\sigma}$.
(iv)~\emph{Noise resilience}: fraction of total variance in the top~80\% of singular values.
\emph{LSE overall quality}: arithmetic mean of the above.

\textbf{LSEX} adds label-aware diagnostics:
(i)~\emph{Cluster compactness}: mean intra-cluster distance / global mean distance.
(ii)~\emph{Inter-cluster gap}: mean nearest-centroid distance between clusters.
(iii)~\emph{$k$-NN purity}: fraction of $k$-nearest neighbours sharing the same label.
(iv)~\emph{Sampling stability}: standard deviation of core quality across 80\% subsamples.

\begin{table}[!t]
    \centering
    \caption{IRALL pseudotime--marker correlations (hematopoiesis).}
    \label{tab:irall_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Hbb-bs} & Erythroid hemoglobin     & $+0.275$ & $< 10^{-53}$ \\
        \textit{Hba-a1} & Erythroid hemoglobin     & $+0.264$ & $< 10^{-49}$ \\
        \textit{Elane}  & Granulocyte elastase     & $+0.255$ & $< 10^{-45}$ \\
        \textit{Cd34}   & HSC / progenitor         & $-0.233$ & $< 10^{-38}$ \\
        \textit{Ctsg}   & Granulocyte cathepsin G  & $+0.226$ & $< 10^{-36}$ \\
        \textit{Cd8a}   & T-cell marker            & $-0.168$ & $< 10^{-20}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Dentate pseudotime--marker correlations (neurogenesis).}
    \label{tab:dentate_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Slc1a3} & Astrocyte/RGC (GLAST)  & $+0.542$ & $< 10^{-228}$ \\
        \textit{Fabp7}  & Astrocyte/RGC (BLBP)   & $+0.476$ & $< 10^{-169}$ \\
        \textit{Sox2}   & Neural stem cell        & $+0.439$ & $< 10^{-141}$ \\
        \textit{Gfap}   & Astrocyte (GFAP)        & $+0.376$ & $< 10^{-101}$ \\
        \textit{Olig1}  & Oligodendrocyte         & $+0.282$ & $< 10^{-56}$  \\
        \textit{Dcx}    & Migrating neuroblast    & $-0.229$ & $< 10^{-37}$  \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Endo pseudotime--marker correlations (pancreatic endocrine).}
    \label{tab:endo_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Ins2}    & $\beta$-cell insulin        & $+0.463$ & $< 10^{-134}$ \\
        \textit{Ins1}    & $\beta$-cell insulin        & $+0.372$ & $< 10^{-83}$  \\
        \textit{Neurog3} & Endocrine progenitor        & $-0.319$ & $< 10^{-61}$  \\
        \textit{Pdx1}    & $\beta$-cell / progenitor TF & $+0.280$ & $< 10^{-47}$  \\
        \textit{Chgb}    & Pan-endocrine               & $+0.191$ & $< 10^{-22}$  \\
        \textit{Gcg}     & $\alpha$-cell glucagon      & $+0.181$ & $< 10^{-20}$  \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Paul pseudotime--marker correlations (myeloid/erythroid).}
    \label{tab:paul_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{Epor}   & Erythropoietin receptor  & $+0.346$ & $< 10^{-77}$ \\
        \textit{Hba-a2} & Erythroid hemoglobin     & $+0.328$ & $< 10^{-69}$ \\
        \textit{Ly6c2}  & Myeloid surface marker   & $+0.272$ & $< 10^{-47}$ \\
        \textit{Elane}  & Granulocyte elastase     & $+0.253$ & $< 10^{-41}$ \\
        \textit{Gata1}  & Erythroid TF             & $+0.231$ & $< 10^{-34}$ \\
        \textit{Gata2}  & Stem/progenitor TF       & $-0.204$ & $< 10^{-27}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!t]
    \centering
    \caption{Spinoids pseudotime--marker correlations (spinal cord organoid).}
    \label{tab:spinoids_markers}
    \begin{tabular}{llcc}
        \toprule
        Gene & Function & Spearman $\rho$ & $p$-value \\
        \midrule
        \textit{MKI67}   & Proliferation (Ki-67)         & $+0.492$ & $< 10^{-183}$ \\
        \textit{TOP2A}   & Proliferation (topoisomerase)  & $+0.438$ & $< 10^{-141}$ \\
        \textit{NES}     & Neural progenitor (nestin)     & $+0.198$ & $< 10^{-28}$  \\
        \textit{TUBB3}   & Neuronal $\beta$-tubulin       & $-0.154$ & $< 10^{-17}$  \\
        \textit{SOX2}    & Neural progenitor              & $+0.136$ & $< 10^{-14}$  \\
        \textit{NEUROG1} & Neurogenin-1                   & $-0.076$ & $< 10^{-5}$   \\
        \bottomrule
    \end{tabular}
\end{table}

%% ============================================================
%% References
%% ============================================================
\balance
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
