# Package-index site source

Next.js App Router static export for this repository's GitHub Pages page.

The export is a code index for the mocoo package. It is not a journal article and must not ship unpublished figures, scores, or a results gallery.

From this directory:

```bash
npm ci
npm run build
npm run verify
```
