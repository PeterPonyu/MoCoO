name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (leave empty for latest)'
        required: false
        default: ''

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: read
      # No id-token permission needed for API token publishing

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Build package
      run: python -m build

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: |
        twine upload dist/*

    - name: Create GitHub release notes
      if: github.event_name == 'release'
      run: |
        echo "## 📦 PyPI Release" >> $GITHUB_STEP_SUMMARY
        echo "- Package: mocoo" >> $GITHUB_STEP_SUMMARY
        echo "- Version: ${{ github.event.release.tag_name }}" >> $GITHUB_STEP_SUMMARY
        echo "- PyPI: https://pypi.org/project/mocoo/" >> $GITHUB_STEP_SUMMARY
        echo "- Release: ${{ github.event.release.html_url }}" >> $GITHUB_STEP_SUMMARY